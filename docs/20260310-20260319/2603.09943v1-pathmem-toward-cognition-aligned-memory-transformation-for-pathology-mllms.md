---
title: "PathMem: Toward Cognition-Aligned Memory Transformation for Pathology MLLMs"
title_zh: PathMem：面向病理多模态大语言模型的认知对齐记忆转换
authors: "Jinyue Li, Yuci Liang, Qiankun Li, Xinheng Lyu, Jiayu Qian, Huabao Chen, Kun Wang, Zhigang Zeng, Anil Anthony Bharath, Yang Liu"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09943v1"
tags: ["query:medllm"]
score: 9.0
evidence: 用于病理诊断推理和知识集成的多模态大模型
tldr: PathMem 是一种受人类病理学家认知过程启发的记忆中心化多模态框架，旨在解决病理大模型在结构化知识集成方面的不足。该框架将病理诊断标准组织为长期记忆，并通过 Memory Transformer 实现从长期记忆到工作记忆的动态转换与上下文感知。实验表明，PathMem 在全切片图像（WSI）报告生成和开放式诊断任务中均达到 SOTA 水平，显著提升了诊断推理的准确性与可解释性。
selection_source: fresh_fetch
motivation: 现有的病理多模态大模型缺乏将结构化领域知识与视觉证据显式结合的机制，导致在复杂诊断推理中难以持续遵循病理学标准。
method: 提出 PathMem 框架，利用 Memory Transformer 模拟从长期记忆到工作记忆的动态转换，实现多模态记忆激活与上下文感知的知识对齐。
result: "PathMem 在 WSI-Bench 基准测试中表现优异，报告生成精度提升了 12.8%，开放式诊断准确率比此前模型提高了 9.7%。"
conclusion: 通过模拟人类层次化记忆过程，PathMem 有效增强了病理大模型在临床诊断场景下的知识整合能力与推理性能。
---

## 摘要
计算病理学不仅需要视觉模式识别，还需要动态整合结构化领域知识，包括分类学、分级标准和临床证据。在实践中，诊断推理需要将形态学证据与正式的诊断和分级标准联系起来。尽管多模态大语言模型（MLLMs）展示了强大的视觉语言推理能力，但它们缺乏结构化知识整合和可解释记忆控制的显式机制。因此，现有模型在推理过程中难以一致地纳入病理特定的诊断标准。受人类病理学家分层记忆过程的启发，我们提出了 PathMem，这是一个面向病理 MLLMs 的以记忆为中心的多模态框架。PathMem 将结构化病理知识组织为长期记忆（LTM），并引入了记忆 Transformer（Memory Transformer），通过多模态记忆激活和上下文感知知识落地（knowledge grounding），模拟从 LTM 到工作记忆（WM）的动态转换，从而实现下游推理的上下文感知记忆细化。PathMem 在各项基准测试中均达到了 SOTA 性能，在 WSI-Bench 报告生成（WSI-Precision 提升 12.8%，WSI-Relevance 提升 10.1%）和开放式诊断方面，分别比之前的基于 WSI 的模型提升了 9.7% 和 8.9%。

## Abstract
Computational pathology demands both visual pattern recognition and dynamic integration of structured domain knowledge, including taxonomy, grading criteria, and clinical evidence. In practice, diagnostic reasoning requires linking morphological evidence with formal diagnostic and grading criteria. Although multimodal large language models (MLLMs) demonstrate strong vision language reasoning capabilities, they lack explicit mechanisms for structured knowledge integration and interpretable memory control. As a result, existing models struggle to consistently incorporate pathology-specific diagnostic standards during reasoning. Inspired by the hierarchical memory process of human pathologists, we propose PathMem, a memory-centric multimodal framework for pathology MLLMs. PathMem organizes structured pathology knowledge as a long-term memory (LTM) and introduces a Memory Transformer that models the dynamic transition from LTM to working memory (WM) through multimodal memory activation and context-aware knowledge grounding, enabling context-aware memory refinement for downstream reasoning. PathMem achieves SOTA performance across benchmarks, improving WSI-Bench report generation (12.8% WSI-Precision, 10.1% WSI-Relevance) and open-ended diagnosis by 9.7% and 8.9% over prior WSI-based models.

---

## 论文详细总结（自动生成）

### PathMem：面向病理多模态大语言模型的认知对齐记忆转换——论文深度总结

#### 1. 核心问题与研究动机
*   **核心问题**：计算病理学诊断不仅依赖于对全切片图像（WSI）的视觉模式识别，更需要动态整合结构化的领域知识（如疾病分类、分级标准、临床证据）。
*   **研究背景**：现有的病理多模态大语言模型（MLLMs）大多是参数化的“黑盒”，缺乏显式的机制来集成结构化知识并进行可解释的记忆控制。
*   **研究动机**：受人类病理学家“长期记忆（LTM）积累知识、工作记忆（WM）处理当前病例”的层次化认知过程启发，旨在构建一个能够模拟这种记忆转换机制的框架，以提升诊断的可靠性和专业性。

#### 2. 方法论
PathMem 框架的核心在于将病理知识图谱化，并通过动态机制实现记忆的提取与转化：
*   **长期记忆（LTM）构建**：
    *   利用 **DeepSearch** 在 PubMed 文献库中进行深度搜索。
    *   通过 LLM 提取“主体-关系-客体”三元组，并结合置信度评分进行过滤。
    *   采用基于哈希的去重和概率多证据融合机制，确保知识库的单调增长与准确性。
*   **记忆 Transformer（Memory Transformer）**：
    *   **静态激活**：通过余弦相似度对知识条目进行初步排名。
    *   **动态激活**：将多模态输入（视觉+文本）与知识嵌入共同投影，计算全局相关性。
    *   **自适应选择**：根据相关性得分确定激活边界，将最相关的 LTM 条目转化为 **工作记忆（WM）**。
*   **推理流程**：将提取的 WM 标记（Tokens）与原始多模态序列拼接，输入 Transformer 解码器，实现基于证据的知识增强推理。

#### 3. 实验设计
*   **数据集**：
    *   **内部基准**：**WSI-Bench**（基于 TCGA，包含 9,850 个 WSI 和约 18 万个 VQA 问答对）。
    *   **外部验证（Zero-shot）**：WSI-VQA、SlideBench-VQA (BCNB)、CPTAC-NSCLC。
*   **对比方法**：
    *   通用大模型：GPT-4o。
    *   病理专用模型：Quilt-LLaVA、WSI-VQA、WSI-LLaVA、SlideChat、Hist-Gen 等。
*   **评估指标**：
    *   语言指标：BLEU-1/4、ROUGE-L、METEOR。
    *   病理专用指标：**WSI-Precision**（病理发现准确度）、**WSI-Relevance**（视觉证据对齐度）。

#### 4. 资源与算力
*   **硬件环境**：使用了 **4 台 NVIDIA A100 GPU**。
*   **训练配置**：
    *   使用 **DeepSpeed** 优化库。
    *   采用 AdamW 优化器，初始学习率 $2 \times 10^{-4}$，配合余弦退火调度。
    *   训练时长为 **3 个 Epoch**。
    *   使用了 **LoRA**（r=128, α=256）进行参数高效微调。

#### 5. 实验数量与充分性
*   **实验规模**：在包含 30 种癌症类型的 18 万个问答对上进行了训练和测试，样本量充足。
*   **消融实验**：详细对比了“仅动态激活”、“仅静态激活”以及“双模态激活”的效果，验证了各组件的必要性。
*   **敏感性分析**：针对激活的知识标记数量（Top-K）进行了 1 到 5 的梯度测试。
*   **客观性**：通过在三个完全独立的外部数据集上进行零样本（Zero-shot）测试，证明了模型的泛化能力和鲁棒性，实验设计较为公平、客观。

#### 6. 主要结论与发现
*   **性能领先**：PathMem 在 WSI-Bench 上的综合表现（0.768）优于所有基准模型。
*   **报告生成突破**：在报告生成任务中，WSI-Precision 提升了 **12.8%**，WSI-Relevance 提升了 **10.1%**。
*   **知识增强的有效性**：定性分析显示，PathMem 能够准确识别复杂病例（如非角化鳞状细胞癌），并能显式引用 LTM 中的诊断标准，减少了幻觉。
*   **记忆转换价值**：动态与静态检索具有互补性，结合使用能达到最优的词法对齐和临床准确性。

#### 7. 优点（亮点）
*   **认知对齐**：首次在病理 MLLM 中引入模拟人类“长期-工作记忆”转换的架构，具有很强的生物学和认知科学解释性。
*   **高质量知识库**：通过自动化流水线从 PubMed 构建了结构化病理知识库，解决了 RAG 方案中知识来源杂乱的问题。
*   **可解释性**：模型在输出诊断结果时，能够展示激活的记忆路径，使 AI 的推理过程对病理医生透明。

#### 8. 不足与局限
*   **知识库覆盖范围**：虽然使用了 PubMed，但对于极罕见病例或最新发表的临床指南，知识库可能仍存在滞后或覆盖不足。
*   **计算开销**：处理吉像素（Gigapixel）级别的 WSI 图像本身耗时较长，引入记忆检索机制后，推理延迟可能会进一步增加。
*   **临床实测缺失**：目前的评估主要基于公开数据集，尚未在真实的临床工作流（如实时辅助诊断）中进行前瞻性验证。

（完）
