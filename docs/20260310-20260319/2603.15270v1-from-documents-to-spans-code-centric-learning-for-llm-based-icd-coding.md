---
title: "From Documents to Spans: Code-Centric Learning for LLM-based ICD Coding"
title_zh: 从文档到片段：基于大语言模型的以代码为中心的 ICD 编码学习
authors: "Xu Zhang, Wenxin Ma, Chenxu Wu, Rongsheng Wang, Kun Zhang, S. Kevin Zhou"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15270v1"
tags: ["query:medllm"]
score: 9.0
evidence: 针对临床文档ICD编码任务微调大语言模型
tldr: 针对ICD编码中代码覆盖有限、缺乏可解释性及长文本微调成本高的问题，本文提出了一种以代码为中心的学习框架（CCL）。该框架将监督重点从完整临床文档转向可扩展的短证据片段，通过混合训练策略和数据扩展，显著降低了训练成本，提升了模型对未知代码的泛化能力和可解释性。实验表明，该方法使小型开源LLM达到了与大型闭源模型相当的性能。
selection_source: fresh_fetch
motivation: 现有的ICD编码微调面临代码覆盖不足、缺乏证据支撑导致的可解释性差以及长文档处理成本高昂的挑战。
method: 提出以代码为中心的学习框架，通过短证据片段进行监督，并结合混合训练策略与数据扩展来优化模型。
result: 该方法在保持可解释性的同时，显著提升了对罕见代码的预测准确率，并使小规模模型表现出媲美巨型商业模型的性能。
conclusion: 将学习重心从文档级转向片段级是提升LLM在医疗编码任务中效率、泛化性和透明度的有效途径。
---

## 摘要
ICD 编码是医疗保健领域一项关键且具有挑战性的任务。近年来，基于大语言模型（LLM）的方法在 ICD 编码中表现出比判别式方法更强的泛化能力。然而，针对 ICD 编码微调 LLM 面临三个主要挑战：首先，现有的公开 ICD 编码数据集对 ICD 代码空间的覆盖有限，限制了模型泛化到未见代码的能力；其次，朴素的微调会降低 LLM 的可解释性，因为很少有公开数据集包含所分配代码的明确支持证据；第三，ICD 编码通常涉及冗长的临床文档，使得微调 LLM 的计算成本非常高。为了解决这些问题，我们提出了“以代码为中心的学习”（Code-Centric Learning），这是一种将监督从完整的临床文档转向可扩展的短证据片段的训练框架。该框架的核心思想是，片段级学习能够提高 LLM 执行文档级 ICD 编码的能力。我们提出的框架由混合训练策略和以代码为中心的数据扩展组成，这显著降低了训练成本，提高了在未见 ICD 代码上的准确性，并保留了可解释性。在相同的 LLM 主干网络下，我们的方法显著优于强基准模型。值得注意的是，我们的方法使小规模 LLM 能够达到与大得多的闭源模型相当的性能，证明了其在全自动 ICD 编码方面的有效性和潜力。

## Abstract
ICD coding is a critical yet challenging task in healthcare. Recently, LLM-based methods demonstrate stronger generalization than discriminative methods in ICD coding. However, fine-tuning LLMs for ICD coding faces three major challenges. First, existing public ICD coding datasets provide limited coverage of the ICD code space, restricting a model's ability to generalize to unseen codes. Second, naive fine-tuning diminishes the interpretability of LLMs, as few public datasets contain explicit supporting evidence for assigned codes. Third, ICD coding typically involves long clinical documents, making fine-tuning LLMs computationally expensive. To address these issues, we propose Code-Centric Learning, a training framework that shifts supervision from full clinical documents to scalable, short evidence spans. The key idea of this framework is that span-level learning improves LLMs' ability to perform document-level ICD coding. Our proposed framework consists of a mixed training strategy and code-centric data expansion, which substantially reduces training cost, improves accuracy on unseen ICD codes and preserves interpretability. Under the same LLM backbone, our method substantially outperforms strong baselines. Notably, our method enables small-scale LLMs to achieve performance comparable to much larger proprietary models, demonstrating its effectiveness and potential for fully automated ICD coding.

---

## 论文详细总结（自动生成）

这是一份关于论文《From Documents to Spans: Code-Centric Learning for LLM-based ICD Coding》的结构化深度分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
ICD 编码是将临床文档转化为标准化疾病诊断代码的过程，对医疗计费和数据分析至关重要。尽管大语言模型（LLM）在这一任务上展现了潜力，但现有的微调方法面临三大挑战：
*   **代码覆盖受限**：公开数据集（如 MIMIC-IV）仅覆盖约 10% 的 ICD 代码，导致模型对罕见或未见代码的泛化能力差。
*   **缺乏可解释性**：标准数据集通常只提供“文档-代码”对，缺乏明确的证据支撑（Evidence Spans），使得模型预测过程黑盒化。
*   **训练效率低下**：临床文档通常极长，直接在长文档上微调 LLM 会带来极高的计算开销（复杂度随长度呈平方增长）。

本文提出了**以代码为中心的学习（Code-Centric Learning, CCL）**框架，核心逻辑是将监督信号从“长文档”转移到“短证据片段”，通过片段级学习来增强模型在文档级的编码能力。

### 2. 方法论：核心思想与关键技术
CCL 框架主要包含两个核心策略：

*   **混合训练策略（Mixed Training Strategy）**：
    *   模型同时在两种数据上训练：1. **文档级数据**（少量高质量标注，要求模型先提取证据再输出代码）；2. **片段级数据**（大量的“证据片段-代码”对）。
    *   **公式化表达**：损失函数结合了文档级 SFT 损失和片段级 SFT 损失。通过这种方式，模型在短片段上学习代码知识，在长文档上学习证据聚合能力。
*   **以代码为中心的数据扩展（Code-Centric Data Expansion）**：
    为了实现 ICD 代码的全覆盖，构建了三层证据知识库：
    1.  **金牌对（Gold Pairs）**：来自官方 ICD 索引和列表的权威术语。
    2.  **银牌对（Silver Pairs）**：利用 LLM（如 Llama-3.1-70B）从带有噪声标签的公开数据集中自动提取和清洗出的证据片段。
    3.  **合成对（Synthetic Pairs）**：针对完全未见的代码，利用 LLM 根据 ICD 层级结构中的邻近代码知识，合成合理的临床证据片段。

### 3. 实验设计
*   **数据集**：
    *   **MIMIC-IV**：作为大规模噪声训练源。
    *   **MDACE**：专家标注的证据级基准，用于评估准确性和可解释性。
    *   **ACI-Bench**：合成临床笔记数据集，作为跨域（Out-of-Distribution）泛化性能的评估。
*   **对比方法（Baselines）**：
    *   **判别式模型**：PLM-ICD, PLM-CA, GKI-ICD（基于 RoBERTa）。
    *   **生成式/LLM 方法**：CoT（思维链）、MAC（多智能体协作）、CLH（Code Like Humans）、以及传统的全文档 SFT。
*   **评估指标**：Micro-F1, Macro-F1, Recall, Precision，以及针对证据提取的 Recall 和 F1。

### 4. 资源与算力
*   **硬件环境**：实验在单张 **NVIDIA H20 GPU (96GB VRAM)** 上完成。
*   **训练时长对比**：
    *   传统全文档 SFT（10万篇文档）：每轮 70 小时，总计约 **210 小时**。
    *   **CCL 框架**（200篇文档 + 15万个片段）：每轮仅 4 小时，总计约 **40 小时**。
*   **效率提升**：训练时间减少了约 **80%**。

### 5. 实验数量与充分性
*   **实验规模**：论文在 Llama-3.1-8B、Qwen3-4B 等多个不同规模和家族的主干模型上验证了 CCL 的有效性。
*   **消融实验**：详细分析了证据注入、金牌/银牌/合成数据对性能的贡献。
*   **分析维度**：包括对“未见代码”的专项准确率分析、人类对合成数据质量的评估（由两名医生参与）、以及人机协作场景下的性能提升测试。
*   **评价**：实验设计全面，涵盖了从性能、效率到可解释性的多维度对比，且在跨域数据集上验证了泛化性，结果较为客观公正。

### 6. 主要结论与发现
*   **片段级学习的有效性**：证明了 LLM 即使只学习短片段，也能显著提升在长文档中的编码表现。
*   **性能跨越**：CCL 使 8B 规模的小模型在多个指标上达到了与 GPT-4 等巨型闭源模型相当甚至更优的水平。
*   **未见代码突破**：在文档训练集中未出现的代码，通过 CCL 的片段扩展，预测准确率从 8.8% 提升至 **50.0%**。
*   **可解释性增强**：模型能够生成与人类专家高度一致的证据片段（证据召回率达 74%）。

### 7. 优点与亮点
*   **范式创新**：打破了“必须用长文档训练长文档任务”的思维定式，通过任务分解（定位证据+分配代码）降低了复杂度。
*   **极高的实用价值**：大幅降低了医疗机构微调本地模型的算力门槛和时间成本。
*   **数据扩展策略**：系统性地解决了 ICD 编码中长尾代码（Rare Codes）覆盖不足的顽疾。
*   **支持人机协作**：通过先输出证据再输出代码的流程，允许医生对证据进行干预和修正，符合医疗场景的安全性需求。

### 8. 不足与局限
*   **合成数据质量**：虽然经过了人类评估，但合成证据在“临床真实感”和“冗余度”上仍有提升空间。
*   **复杂逻辑缺失**：目前的片段学习主要侧重于术语映射，对于 ICD 编码规则中复杂的逻辑（如“排除规则”、“合并编码指令”）涉及较少。
*   **语言局限**：实验主要集中在英文数据集，对于其他语种（如中文）的临床术语和编码习惯的适用性有待验证。

（完）
