---
title: Unleashing Video Language Models for Fine-grained HRCT Report Generation
title_zh: 释放视频语言模型在细粒度 HRCT 报告生成中的潜力
authors: "Yingying Fang, Huichi Zhou, KinHei Lee, Yijia Wang, Zhenxuan Zhang, Jiahao Huang, Guang Yang"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12469v1"
tags: ["query:medllm"]
score: 9.0
evidence: 生成式AI用于HRCT诊断报告生成
tldr: 针对高分辨率CT（HRCT）报告生成中病理多样性和空间稀疏性的挑战，本文提出AbSteering框架。该框架利用视频语言模型（VideoLMs）的时空推理能力，通过异常中心思维链（CoT）和直接偏好优化（DPO）引导模型进行精细化诊断。实验证明，该方法在检测灵敏度和减少幻觉方面优于现有的领域专用CT大模型，展示了通用模型在医疗领域的强大迁移潜力。
selection_source: fresh_fetch
motivation: 旨在解决HRCT影像中病理多样性高且空间稀疏导致的报告生成难题，探索通用视频语言模型在复杂医疗影像解释中的适应性。
method: 引入了AbSteering框架，结合了增强异常推理的思维链（CoT）方案和利用易混淆病例作为负样本的直接偏好优化（DPO）策略。
result: 实验结果显示，AbSteering在检测灵敏度上优于现有的领域专用CT基础模型，并有效缓解了诊断报告中的幻觉问题。
conclusion: 研究证明通过特定的引导范式，通用视频语言模型可以成功迁移至高容量医疗影像任务，实现优于专用模型的精细化诊断性能。
---

## 摘要
从高分辨率计算机断层扫描（HRCT）生成精确的诊断报告对临床工作流至关重要，但由于 3D 体数据中高度的病理多样性和空间稀疏性，这仍然是一个巨大的挑战。尽管视频语言模型（VideoLMs）在通用领域展示了卓越的时空推理能力，但其在特定领域、大体量医学影像解读中的适应性仍有待探索。在这项工作中，我们提出了 AbSteering，这是一个以异常为中心的框架，旨在引导 VideoLMs 进行精确的 HRCT 报告生成。具体而言，AbSteering 引入了：（i）一种以异常为中心的思维链（Chain-of-Thought）方案，用于强化异常推理；（ii）一种直接偏好优化（Direct Preference Optimization）目标，利用临床上易混淆的异常作为难负样本（hard negatives），以增强细粒度辨别能力。我们的结果表明，在这一范式的引导下，通用 VideoLMs 对大体量医学影像具有很强的迁移能力。值得注意的是，AbSteering 的表现优于目前最先进的、基于大规模 CT 预训练的领域特定 CT 基础模型，在实现更高检测灵敏度的同时，有效缓解了幻觉问题。我们的数据和模型权重已发布在 https://anonymous.4open.science/r/hrct-report-generation-video-vlm-728C/。

## Abstract
Generating precise diagnostic reports from High-Resolution Computed Tomography (HRCT) is critical for clinical workflow, yet it remains a formidable challenge due to the high pathological diversity and spatial sparsity within 3D volumes. While Video Language Models (VideoLMs) have demonstrated remarkable spatio-temporal reasoning in general domains, their adaptability to domain-specific, high-volume medical interpretation remains underexplored. In this work, we present AbSteering, an abnormality-centric framework that steers VideoLMs toward precise HRCT report generation. Specifically, AbSteering introduces: (i) an abnormality-centric Chain-of-Thought scheme that enforces abnormality reasoning, and (ii) a Direct Preference Optimization objective that utilizes clinically confusable abnormalities as hard negatives to enhance fine-grained discrimination. Our results demonstrate that general-purpose VideoLMs possess strong transferability to high-volume medical imaging when guided by this paradigm. Notably, AbSteering outperforms state-of-the-art domain-specific CT foundation models, which are pretrained with large-scale CTs, achieving superior detection sensitivity while simultaneously mitigating hallucinations. Our data and model weights are released at https://anonymous.4open.science/r/hrct-report-generation-video-vlm-728C/

---

## 论文详细总结（自动生成）

以下是对论文《Unleashing Video Language Models for Fine-grained HRCT Report Generation》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：高分辨率 CT（HRCT）是诊断胸部疾病的金标准，但其 3D 体数据包含数百张切片，病灶细微且空间分布稀疏，人工撰写报告负担重且存在观察者差异。
*   **核心问题**：现有的自动报告生成模型多依赖于昂贵的领域专用编码器（如专用 CT 基础模型），且在识别长尾、细微异常方面表现不佳。
*   **研究动机**：视频语言模型（VideoLMs）在处理时空序列方面表现卓越，而 3D CT 卷在结构上与视频高度相似。本文旨在探索通用 VideoLMs 迁移至医学影像领域的潜力，并解决其缺乏医学专业知识的问题。

### 2. 方法论
论文提出了 **AbSteering** 框架，通过两个阶段引导通用 VideoLM 进行精准诊断：
*   **核心思想**：利用结构化推理路径和判别式偏好优化，将通用时空推理能力转化为细粒度的医学诊断能力。
*   **关键技术细节**：
    1.  **以异常为中心的思维链（Abnormality-centric CoT, Stage 1）**：
        *   将原始报告重构为“解剖区域：异常发现”的结构化模板（如：肺部：磨玻璃影）。
        *   训练模型遵循“检测异常 -> 总结报告”的因果链，强制模型在生成叙述前先进行病理推理。
    2.  **细粒度异常判别优化（DPO, Stage 2）**：
        *   **硬负样本构造**：利用 GPT-4o 将真实报告中的异常替换为临床上易混淆的替代品（如同区域但性质不同的病变），生成“假报告”。
        *   **直接偏好优化（DPO）**：通过公式 $L_{DPO}$ 训练模型，使其在真实报告（正样本）和临床误导性报告（负样本）之间进行选择，从而强化模型对细微视觉差异的辨别力，抑制幻觉。

### 3. 实验设计
*   **数据集**：使用 **CT-RATE** 数据集（包含 25,692 个胸部 CT 卷及配对报告）。作者进一步整理出 **CT-RATE-AB** 变体，用于结构化 CoT 训练。
*   **Benchmark（基准）**：
    *   **非 LLM 模型**：CT2Rep。
    *   **领域专用基础模型**：RadFM、M3D-8B、CT-CHAT、Reg2RG。
    *   **通用 VideoLM 基座**：Qwen2.5-VL-7B、InternVL3-8B。
*   **对比方法**：将上述模型在相同数据集上进行微调，对比其在应用 AbSteering 框架前后的性能差异。

### 4. 资源与算力
*   **硬件设备**：使用了 **2 张 80GB NVIDIA A100 GPU**。
*   **训练配置**：总 Batch Size 为 4。
*   **训练时长**：文中未明确给出具体的训练总小时数，但提到在两阶段微调中使用了上述算力资源。

### 5. 实验数量与充分性
*   **实验规模**：对比了 7 种主流模型，涵盖了从专用到通用的多种架构。
*   **消融实验**：
    *   验证了 CoT 和 DPO 各自的增益。
    *   对比了不同的视觉编码器策略（从头训练、冻结权重、LoRA 微调）。
    *   探索了 LLM 参数规模（3B, 7B, 32B）对结果的影响。
*   **充分性与客观性**：实验设计较为全面，不仅使用了传统的自然语言指标（BLEU, ROUGE），还引入了基于预训练分类器的临床效能（CE）指标，能够更客观地反映诊断的准确性。

### 6. 主要结论与发现
*   **迁移能力强**：通用视频语言模型（如 InternVL3）在医学影像任务上展现出极强的迁移潜力，其基准表现甚至优于部分专门在医疗数据上预训练的模型。
*   **框架有效性**：AbSteering 显著提升了模型的临床指标，尤其是大幅提高了异常检测的召回率（Recall），并有效减少了诊断幻觉。
*   **预训练价值**：实验证明通用视频预训练权重不可或缺，直接从头训练效果极差；同时发现 7B 规模的模型在当前任务中性价比最高，32B 模型反而因对齐难度增加导致性能下降。

### 7. 优点
*   **高效性**：提供了一种无需从头训练昂贵医疗基础模型即可获得高性能报告生成器的方法。
*   **逻辑严密**：DPO 阶段引入“临床易混淆异常”作为负样本，非常贴合放射科医生的实际诊断逻辑。
*   **开源贡献**：发布了处理后的结构化数据集 CT-RATE-AB，有助于后续研究。

### 8. 不足与局限
*   **数据集单一**：实验主要集中在 CT-RATE 数据集上，缺乏跨中心、跨设备的多源数据验证。
*   **依赖外部模型**：负样本的构造和报告结构化高度依赖 GPT-4o，可能引入外部模型的偏见或错误。
*   **计算门槛**：虽然比预训练省力，但微调大型 VideoLM 仍需要 A100 等高端显卡，对基层医疗机构的部署仍有挑战。

（完）
