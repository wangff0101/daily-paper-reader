---
title: "EviAgent: Evidence-Driven Agent for Radiology Report Generation"
title_zh: EviAgent：证据驱动的放射学报告生成智能体
authors: "Tuoshi Qi, Shenshen Bu, Yingfei Xiang, Zhiming Dai"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13956v1"
tags: ["query:medllm"]
score: 9.0
evidence: 用于放射报告生成和诊断的多模态大模型智能体
tldr: 针对放射科报告生成中多模态大模型存在的“黑盒”决策和缺乏外部知识等问题，本文提出证据驱动代理 EviAgent。该方法将复杂的生成过程分解为细粒度操作，整合多维视觉专家和检索机制，为诊断提供显式视觉证据和高质量临床先验。实验证明其在多个主流数据集上优于现有模型，提供了更具可信度的自动化报告生成方案。
selection_source: fresh_fetch
motivation: 现有多模态大模型在生成放射报告时缺乏透明的视觉证据支持且难以获取外部领域知识。
method: 提出 EviAgent，通过分解生成流程并集成视觉专家与检索模块，实现证据驱动的透明推理。
result: 在 MIMIC-CXR、CheXpert Plus 和 IU-Xray 数据集上的实验结果显示，EviAgent 的性能超越了通用及专用医疗模型。
conclusion: EviAgent 为自动化放射报告生成提供了一种鲁棒且可信的解决方案，有效提升了诊断的透明度。
---

## 摘要
自动放射学报告生成在减轻放射科医生沉重工作量方面具有巨大潜力。尽管近期的多模态大语言模型（MLLMs）具备强大的视觉语言能力，但其临床部署受到固有局限性的严重制约：其“黑盒”决策机制由于缺乏支持诊断的显式视觉证据，导致生成的报告不可追溯，且难以获取外部领域知识。为了应对这些挑战，我们提出了证据驱动的放射学报告生成智能体（EviAgent）。与不透明的端到端范式不同，EviAgent 通过将复杂的生成过程分解为细粒度的操作单元，协调出透明的推理轨迹。我们集成了多维视觉专家和检索机制作为外部支持模块，赋予系统显式的视觉证据和高质量的临床先验。在 MIMIC-CXR、CheXpert Plus 和 IU-Xray 数据集上的广泛实验表明，EviAgent 的性能优于大规模通用模型和专业医学模型，为自动放射学报告生成提供了一种稳健且值得信赖的解决方案。

## Abstract
Automated radiology report generation holds immense potential to alleviate the heavy workload of radiologists. Despite the formidable vision-language capabilities of recent Multimodal Large Language Models (MLLMs), their clinical deployment is severely constrained by inherent limitations: their "black-box" decision-making renders the generated reports untraceable due to the lack of explicit visual evidence to support the diagnosis, and they struggle to access external domain knowledge. To address these challenges, we propose the Evidence-driven Radiology Report Generation Agent (EviAgent). Unlike opaque end-to-end paradigms, EviAgent coordinates a transparent reasoning trajectory by breaking down the complex generation process into granular operational units. We integrate multi-dimensional visual experts and retrieval mechanisms as external support modules, endowing the system with explicit visual evidence and high-quality clinical priors. Extensive experiments on MIMIC-CXR, CheXpert Plus, and IU-Xray datasets demonstrate that EviAgent outperforms both large-scale generalist models and specialized medical models, providing a robust and trustworthy solution for automated radiology report generation.

---

## 论文详细总结（自动生成）

以下是对论文《EviAgent: Evidence-Driven Agent for Radiology Report Generation》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的多模态大语言模型（MLLMs）在自动生成放射学报告时存在两大瓶颈：
    1.  **“黑盒”决策**：端到端的生成方式缺乏显式的视觉证据支持，导致诊断结论不可追溯，难以在临床中建立信任。
    2.  **知识局限**：模型主要依赖内部参数化知识，难以实时获取外部医学领域知识或临床先验。
*   **研究背景**：放射科医生工作量巨大，自动化报告生成需求迫切。虽然通用模型（如 GPT-5.1）和医学专用模型（如 Lingshu）表现出色，但仍无法解决证据链缺失和隐私合规（云端 API 风险）的问题。

### 2. 论文提出的方法论：EviAgent
EviAgent 是一个**无需额外训练**的证据驱动智能体框架，其核心思想是将报告生成从“像素到文本”的直接映射转变为“计划-行动-证据-报告”的结构化推理过程。
*   **核心架构**：
    1.  **规划与分解（Planning）**：Planner（基于 Qwen3-VL-8B）将复杂的诊断任务分解为细粒度的操作单元（如“先检测病变，再定位疾病”）。
    2.  **工具增强的 ReAct 循环**：通过 ReAct 模式动态调用工具箱。工具箱包含：
        *   **感知专家**：Swin-Transformer（分类）、CheXagent（体位识别）、MAIRA-2（视觉接地/定位）、MedSAM（解剖分割）。
        *   **知识检索（RAG）**：基于 14 种病理构建知识库，利用多模态嵌入模型检索相似历史案例。
        *   **自定义扩展**：通过模型上下文协议（MCP）支持插件式集成。
    3.  **证据提取（Evidence Extraction）**：从冗长的交互历史中过滤噪声，提取分类概率、坐标和临床先验，形成结构化证据链。
    4.  **最终输出（Output）**：严格基于提取的证据生成最终报告，确保言之有据。

### 3. 实验设计
*   **数据集**：
    *   **MIMIC-CXR**：大型胸部 X 光数据集（官方测试集 3,858 张图像）。
    *   **IU-Xray**：印第安纳大学数据集（测试集 1,180 张图像）。
    *   **CheXpert Plus**：包含文本报告和人口统计学信息的增强数据集。
*   **评估指标**：
    *   **领域特定指标**：RaTEScore（实体感知）、SembScore（临床内容一致性）、RadCliQ⁻¹（综合词法、语义和临床关系）。
    *   **LLM-as-a-Judge**：使用 DeepSeek-V3.2 从准确性、定位、专业性和临床可接受度四个维度评分。
*   **对比方法**：
    *   **闭源通用模型**：GPT-5.1、Claude 4.5 Sonnet、Gemini-2.5-Flash。
    *   **医学专用模型**：LLaVA-Med、HuatuoGPT-V、MedGemma、Lingshu 等。
    *   **开源通用模型**：InternVL 系列、Qwen-VL 系列。

### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA H100 GPU** 上运行。
*   **推理引擎**：使用 **vLLM** 库进行高吞吐量、显存高效的推理。
*   **训练时长**：由于 EviAgent 是一个基于 Prompt 工程和工具集成的智能体框架，**不需要额外的模型训练**，因此未提及训练时长。系统完全在本地（On-premise）运行以保护隐私。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流数据集上进行了全面测试，涵盖了从 8B 到超大规模参数的 11 种基准模型。
*   **消融实验**：
    *   验证了分类、定位、检索三个核心模块的贡献（发现分类专家对防止错误传播最关键）。
    *   验证了“规划”和“证据提取”机制的必要性。
*   **定性分析**：通过复杂病例展示了 EviAgent 在复杂场景下的精确度以及错误的可追溯性（即可以定位是哪个工具出了错，而不是模型在胡编）。
*   **充分性评价**：实验设计客观、公平，对比了当前最顶尖的闭源模型，且使用了最新的医学专用评估指标，具有很强的说服力。

### 6. 主要结论与发现
*   **性能领先**：EviAgent 在几乎所有指标上均优于现有模型。在 MIMIC-CXR 上，其 SembScore 甚至超过了 GPT-5.1 和专门微调过的 Lingshu-7B。
*   **可追溯性**：通过证据链，医生可以清晰看到报告中的每一句话对应哪个工具的输出（如具体的 bounding box 或检索到的先验），解决了“幻觉”问题。
*   **本地化优势**：使用 8B 规模的本地模型作为 Planner，配合专用工具，即可达到甚至超越云端巨型模型的医疗诊断水平，且保障了数据隐私。

### 7. 优点
*   **透明度高**：将黑盒推理转变为白盒证据链，符合医疗诊断的严谨性要求。
*   **隐私保护**：全本地化部署，不依赖外部 API。
*   **可扩展性强**：通过 MCP 协议可以轻松集成新的医学影像处理工具。
*   **即插即用**：无需昂贵的微调过程，通过智能体协作提升了基础模型的能力上限。

### 8. 不足与局限
*   **推理延迟**：由于需要多轮交互（规划、多次工具调用、证据提取），其推理速度明显慢于端到端的单次前向传播模型。
*   **工具依赖风险**：报告的准确性高度依赖于底层专家工具（如分类器和定位模型）的质量。如果工具本身产生错误证据，Planner 可能会忠实地整合这些错误。
*   **计算开销**：虽然不需要训练，但多轮推理和多个专家模型的并发运行对显存和计算资源仍有一定要求。

（完）
