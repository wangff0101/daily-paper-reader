---
title: "Tabular LLMs for Interpretable Few-Shot Alzheimer's Disease Prediction with Multimodal Biomedical Data"
title_zh: 用于多模态生物医学数据可解释少样本阿尔茨海默病预测的表格大语言模型
authors: "Sophie Kearney, Shu Yang, Zixuan Wen, Weimin Lyu, Bojian Hou, Duy Duong-Tran, Tianlong Chen, Jason H. Moore, Marylyn D. Ritchie, Chao Chen, Li Shen"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.17191v1"
tags: ["query:medllm"]
score: 9.0
evidence: 微调表格大模型用于阿尔茨海默症预测
tldr: 针对阿尔茨海默病（AD）诊断中表格生物标志物数据规模小且不完整的挑战，本研究提出了TAP-GPT。该框架基于TableGPT2，通过表格提示词而非纯文本进行微调，实现了少样本下的多模态AD分类。实验表明，TAP-GPT在少样本场景下优于传统机器学习模型，且无需填补即可处理缺失数据，并能生成符合生物学逻辑的可解释推理，为临床决策支持系统奠定了基础。
selection_source: fresh_fetch
motivation: 旨在解决深度学习模型在处理小规模、不完整的阿尔茨海默病表格数据时表现不佳且缺乏可解释性的问题。
method: 提出了基于TableGPT2微调的TAP-GPT框架，利用表格化提示词对多模态生物标志物数据进行少样本分类预测。
result: TAP-GPT在少样本设定下超越了传统机器学习基线，在数据缺失情况下表现稳定，并能提供结构化的临床推理。
conclusion: 该研究证明了专门化的表格大语言模型在处理结构化临床预测任务中的有效性，为多智能体临床决策系统提供了新思路。
---

## 摘要
阿尔茨海默病 (AD) 的准确诊断需要处理表格形式的生物标志物数据，然而此类数据通常规模较小且不完整，深度学习模型在这些数据上的表现往往难以超越经典方法。预训练大语言模型 (LLMs) 提供了少样本泛化、结构化推理和可解释的输出，为临床预测带来了强大的范式转变。我们提出了 TAP-GPT（表格阿尔茨海默病预测 GPT），这是一个基于 TableGPT2 构建的领域自适应表格 LLM 框架，并针对使用表格提示（而非纯文本）的少样本 AD 分类进行了微调。我们在四个源自 ADNI 的数据集上评估了 TAP-GPT，包括用于二元 AD 分类的 QT-PAD 生物标志物以及区域级结构化 MRI、淀粉样蛋白 PET 和 tau PET。在多模态和单模态设置下，TAP-GPT 相比其骨干模型有所改进，在少样本设置中优于传统的机器学习基准，同时与最先进的通用 LLMs 相比仍具竞争力。我们展示了特征选择可以减轻高维输入带来的性能下降，并且 TAP-GPT 在模拟和真实缺失情况下无需插补即可保持稳定的性能。此外，TAP-GPT 产生了与既定 AD 生物学一致的结构化、模态感知的推理，并在自我反思下表现出更高的稳定性，支持其在迭代多智能体系统中的应用。据我们所知，这是表格专用 LLM 在基于多模态生物标志物的 AD 预测中的首次系统应用，证明了此类预训练模型可以有效处理结构化临床预测任务，并为表格 LLM 驱动的多智能体临床决策支持系统奠定了基础。源代码可在 GitHub 上公开获取：https://github.com/sophie-kearney/TAP-GPT。

## Abstract
Accurate diagnosis of Alzheimer's disease (AD) requires handling tabular biomarker data, yet such data are often small and incomplete, where deep learning models frequently fail to outperform classical methods. Pretrained large language models (LLMs) offer few-shot generalization, structured reasoning, and interpretable outputs, providing a powerful paradigm shift for clinical prediction. We propose TAP-GPT Tabular Alzheimer's Prediction GPT, a domain-adapted tabular LLM framework built on TableGPT2 and fine-tuned for few-shot AD classification using tabular prompts rather than plain texts. We evaluate TAP-GPT across four ADNI-derived datasets, including QT-PAD biomarkers and region-level structural MRI, amyloid PET, and tau PET for binary AD classification. Across multimodal and unimodal settings, TAP-GPT improves upon its backbone models and outperforms traditional machine learning baselines in the few-shot setting while remaining competitive with state-of-the-art general-purpose LLMs. We show that feature selection mitigates degradation in high-dimensional inputs and that TAP-GPT maintains stable performance under simulated and real-world missingness without imputation. Additionally, TAP-GPT produces structured, modality-aware reasoning aligned with established AD biology and shows greater stability under self-reflection, supporting its use in iterative multi-agent systems. To our knowledge, this is the first systematic application of a tabular-specialized LLM to multimodal biomarker-based AD prediction, demonstrating that such pretrained models can effectively address structured clinical prediction tasks and laying the foundation for tabular LLM-driven multi-agent clinical decision-support systems. The source code is publicly available on GitHub: https://github.com/sophie-kearney/TAP-GPT.

---

## 论文详细总结（自动生成）

以下是对论文《Tabular LLMs for Interpretable Few-Shot Alzheimer's Disease Prediction with Multimodal Biomedical Data》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：阿尔茨海默病（AD）的诊断依赖于多模态生物标志物（如AT[N]系统：淀粉样蛋白、Tau蛋白、神经退行性变）。
*   **核心挑战**：临床生物医学数据通常以表格形式存在，具有**样本量小（Few-shot）**、**特征维度高**、**数据缺失严重**等特点。传统的深度学习模型在小规模表格数据上往往表现不如随机森林或XGBoost，且缺乏临床决策所需的可解释性。
*   **研究动机**：探索预训练表格大语言模型（Tabular LLMs）是否能通过少样本学习和结构化推理，解决AD预测中的数据稀疏性与可解释性难题。

### 2. 方法论
*   **核心框架（TAP-GPT）**：基于 **TableGPT2-7B** 构建，该模型结合了语义表格编码器（Table Encoder）和大语言模型解码器（Qwen2.5-7B）。
*   **关键技术细节**：
    *   **参数高效微调（PEFT）**：使用 **QLoRA** 技术对解码器进行微调，而保持表格编码器固定，以注入AD领域的诊断知识。
    *   **表格化提示词（Tabular Prompting）**：不同于将表格转为纯文本的序列化方法，TAP-GPT 直接在提示词中使用类似 `df.head()` 的结构化表格格式，保留列对齐信息。
    *   **特征选择**：针对高维影像数据，引入 **LASSO** 或 **LLM引导的排序**，将特征降维至最优区间（如16个特征），以适配LLM的上下文长度限制。
    *   **可解释推理**：通过思维链（CoT）提示，要求模型输出包含预测标签、推理过程和置信度评分的 JSON 对象。
    *   **约束解码**：在推理时限制输出空间为二进制标签（0或1），确保预测的规范性。

### 3. 实验设计
*   **数据集**：源自 ADNI 数据库的四个数据集：
    1.  **QT-PAD**：包含15个临床标志物（PET、CSF、MRI、遗传、人口学）。
    2.  **区域影像数据**：分别针对淀粉样蛋白 PET、Tau PET 和结构化 MRI 的 74 个区域汇总特征。
*   **基准方法（Benchmarks）**：
    *   **传统机器学习**：逻辑回归（LR）、随机森林（RF）、XGBoost、SVM。
    *   **表格基础模型**：TabPFN。
    *   **通用 LLMs**：Qwen2.5-7B-Instruct、Qwen3-8B、GPT-4o-mini。
*   **评估指标**：F1 分数（主要）、平衡准确率、精确率和召回率。

### 4. 资源与算力
*   **硬件环境**：使用高性能计算集群，配备 **NVIDIA A100 (80GB)** GPU。
*   **算力消耗**：每个训练任务分配 1 个 GPU 和 160GB 系统内存。
*   **实验规模**：执行了数百个 GPU 任务，涵盖了 335 个实验变体（不同模态、种子、提示格式和超参数组合），累计产生约 0.25 TB 的模型检查点。

### 5. 实验数量与充分性
*   **实验规模**：非常充分。研究涵盖了 4 种模态、10 个随机种子以确保统计显著性。
*   **消融实验**：
    *   **k-Ablation**：测试了不同数量的上下文示例（k 从 2 到 20）。
    *   **p-Ablation**：测试了不同特征维度（p 为 8, 16, 32）对性能的影响。
*   **鲁棒性测试**：模拟了 10% 到 50% 的数据缺失，并使用了包含真实缺失值的独立临床队列进行验证。
*   **客观性**：严格划分了训练集、验证集、测试集以及专门用于提示词构造的 ICL（上下文学习）池，确保无数据泄露。

### 6. 主要结论与发现
*   **性能优越**：在少样本（如 4-shot 或 8-shot）设定下，TAP-GPT 的 F1 分数显著优于传统机器学习模型和 TabPFN。
*   **结构化优势**：在少样本场景下，表格化提示词（Tabular）比纯文本序列化（Serialized）更有效。
*   **缺失值鲁棒性**：TAP-GPT 无需数据插补即可处理缺失值，在缺失率高达 40% 时仍能保持稳定性能。
*   **生物学一致性**：模型生成的推理过程能够正确引用海马体体积、APOE4 状态等关键生物标志物，符合已知的 AD 病理学逻辑。
*   **稳定性**：在自我反思（Self-reflection）实验中，TAP-GPT 比 GPT-4o-mini 表现出更高的预测稳定性。

### 7. 优点
*   **领域适配性强**：首次将表格专用 LLM 系统应用于多模态 AD 预测，填补了医疗表格数据处理的空白。
*   **端到端处理**：能够直接处理含有缺失值的原始表格，简化了临床数据预处理流程。
*   **高可解释性**：不仅提供分类结果，还提供结构化的临床推理，为“白盒”医疗 AI 提供了范例。
*   **少样本能力**：在极小样本量下展现出强大的泛化能力，适合罕见病或昂贵检测数据的建模。

### 8. 不足与局限
*   **骨干模型限制**：受限于 TableGPT2 的架构，无法轻易更换更先进的解码器（如 Qwen3），且表格编码器在微调中保持固定，可能限制了特征提取的上限。
*   **推理一致性风险**：定性分析发现模型偶尔会误解特征方向（如误将高 FDG 视为 AD 迹象）或产生幻觉。
*   **高维瓶颈**：尽管有特征选择，但 LLM 的上下文窗口仍限制了其处理超大规模表格（如全基因组数据）的能力。
*   **应用限制**：目前仅限于二元分类（CN vs. AD），尚未扩展到轻度认知障碍（MCI）转化预测等更复杂的临床场景。

（完）
