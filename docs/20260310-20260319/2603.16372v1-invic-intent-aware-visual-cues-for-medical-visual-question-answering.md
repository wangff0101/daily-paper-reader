---
title: "InViC: Intent-aware Visual Cues for Medical Visual Question Answering"
title_zh: InViC：面向医学视觉问答的意图感知视觉提示
authors: "Zhisong Wang, Ziyang Chen, Zanting Ye, Hongze Zhu, Yefeng Zheng, Yong Xia"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16372v1"
tags: ["query:medllm"]
score: 9.0
evidence: 增强多模态大语言模型中的医学视觉问答
tldr: 针对医疗视觉问答（Med-VQA）中模型过度依赖语言先验而忽视视觉证据的问题，本文提出了一种轻量级插件框架 InViC。该框架通过提取与问题意图相关的视觉提示（Cue Tokens），并结合两阶段瓶颈注意力微调策略，强制模型关注关键视觉信息。在多个公开数据集上的实验证明，InViC 显著提升了 Med-VQA 的准确性和可靠性。
selection_source: fresh_fetch
motivation: 现有的多模态大模型在医疗问答中常出现“捷径回答”现象，即过度依赖文本偏见而忽略了对临床诊断至关重要的细微视觉特征。
method: 提出 InViC 框架，利用 CTE 模块提取问题相关的视觉提示，并通过两阶段瓶颈注意力掩码策略引导模型深度融合视觉证据。
result: 在 VQA-RAD、SLAKE 和 ImageCLEF 等主流数据集上，InViC 的表现均优于零样本推理和标准的 LoRA 微调方法。
conclusion: 通过引入意图感知的视觉提示和瓶颈训练策略，可以有效减少模型对语言先验的依赖，提升医疗视觉问答的可信度。
---

## 摘要
医学视觉问答 (Med-VQA) 旨在根据医学图像回答临床相关问题。然而，现有的多模态大语言模型 (MLLMs) 经常表现出“捷径回答”行为，即通过利用语言先验或数据集偏差生成看似合理的回答，而对视觉证据的关注不足。这种行为损害了临床可靠性，尤其是在细微的影像学发现具有决定性意义时。我们提出了一个名为“意图感知视觉提示”(InViC) 的轻量级插件框架，旨在显式增强医学 VQA 中基于图像的答案生成。InViC 引入了一个提示标记提取 (CTE) 模块，将密集的视觉标记蒸馏为一组紧凑的 K 个以问题为条件的提示标记，这些标记作为结构化的视觉中间体注入 LLM 解码器，以促进与意图对齐的视觉证据。为了防止模型绕过视觉信息，我们进一步设计了一种带有提示瓶颈注意力掩码的两阶段微调策略。在第一阶段，我们使用注意力掩码来阻断 LLM 对原始视觉特征的直接观察，从而使所有视觉证据都通过提示路径传输。在第二阶段，恢复标准因果注意力，以训练 LLM 共同利用视觉标记和提示标记。我们在多个代表性 MLLM 上，针对三个公开的 Med-VQA 基准数据集（VQA-RAD、SLAKE 和 ImageCLEF VQA-Med 2019）对 InViC 进行了评估。结果表明，InViC 相比零样本推理和标准 LoRA 微调均有持续提升，证明了带有瓶颈训练的意图感知视觉提示是提高可信 Med-VQA 的一种实用且有效的策略。

## Abstract
Medical visual question answering (Med-VQA) aims to answer clinically relevant questions grounded in medical images. However, existing multimodal large language models (MLLMs) often exhibit shortcut answering, producing plausible responses by exploiting language priors or dataset biases while insufficiently attending to visual evidence. This behavior undermines clinical reliability, especially when subtle imaging findings are decisive. We propose a lightweight plug-in framework, termed Intent-aware Visual Cues (InViC), to explicitly enhance image-based answer generation in medical VQA. InViC introduces a Cue Tokens Extraction (CTE) module that distills dense visual tokens into a compact set of K question-conditioned cue tokens, which serve as structured visual intermediaries injected into the LLM decoder to promote intent-aligned visual evidence. To discourage bypassing of visual information, we further design a two-stage fine-tuning strategy with a cue-bottleneck attention mask. In Stage I, we employ an attention mask to block the LLM's direct view of raw visual features, thereby funneling all visual evidence through the cue pathway. In Stage II, standard causal attention is restored to train the LLM to jointly exploit the visual and cue tokens. We evaluate InViC on three public Med-VQA benchmarks (VQA-RAD, SLAKE, and ImageCLEF VQA-Med 2019) across multiple representative MLLMs. InViC consistently improves over zero-shot inference and standard LoRA fine-tuning, demonstrating that intent-aware visual cues with bottlenecked training is a practical and effective strategy for improving trustworthy Med-VQA.

---

## 论文详细总结（自动生成）

### 论文总结：InViC - 面向医学视觉问答的意图感知视觉提示

#### 1. 核心问题与研究动机
医学视觉问答（Med-VQA）要求模型根据医学图像（如 CT、MRI）回答临床问题。然而，现有的多模态大语言模型（MLLM）在处理此类任务时存在**“捷径回答”（Shortcut Answering）**的严重问题：模型倾向于利用语言先验（如问题的固定套路）或数据集偏差来生成看似合理的答案，而忽略了对临床诊断至关重要的细微视觉证据。这种行为降低了模型在医疗场景中的可靠性。

#### 2. 方法论
论文提出了一种名为 **InViC（Intent-aware Visual Cues）** 的轻量级插件框架，核心思想是通过结构化约束强制模型关注视觉信息。
*   **提示标记提取（CTE）模块**：
    *   **初始化**：从问题的池化表示中初始化 $K$ 个可学习的种子槽位（Seed Slots），使其具备意图感知能力。
    *   **跨模态检索**：通过交叉注意力机制，让槽位先关注文本标记（理解问题），再关注视觉标记（提取相关影像证据），将密集的视觉信息蒸馏为紧凑的 $K$ 个“提示标记”（Cue Tokens）。
    *   **残差校准**：使用带有门控机制的适配器结构对提示标记进行精炼。
*   **两阶段瓶颈训练策略**：
    *   **第一阶段（瓶颈训练）**：引入特殊的注意力掩码，阻断 LLM 解码器直接观察原始视觉标记的路径，强制所有视觉信息必须通过“提示标记”这一窄通道传输。
    *   **第二阶段（全上下文微调）**：恢复标准的因果注意力，允许模型同时利用原始视觉标记和提示标记进行联合推理，以优化生成质量。

#### 3. 实验设计
*   **数据集**：
    *   **VQA-RAD**：放射学数据集，包含解剖、属性及异常相关问题。
    *   **SLAKE**：多模态、中英双语医学问答数据集。
    *   **ImageCLEF VQA-Med 2019**：专注于开放式医学问答的基准。
*   **对比基准（Benchmarks）**：
    *   **闭源模型**：GPT-4o、GPT-4o-mini。
    *   **医疗专用模型**：Hulu-Med、Lingshu-7B。
    *   **通用 MLLM**：Qwen3-VL (4B/8B)、InternVL3.5 (4B/8B)。
    *   **微调方法**：标准的 LoRA 全局微调。
*   **评价指标**：准确率（Accuracy）、召回率（Recall）、BLEU 分数。针对开放式问题，采用 GPT-4o-mini 作为评判员（LLM-as-a-judge）。

#### 4. 资源与算力
*   **硬件**：使用了 8 张 **NVIDIA GeForce RTX 4090 GPU**。
*   **训练细节**：
    *   Batch Size 为 8。
    *   第一阶段训练 2 个 Epoch（仅训练 CTE 模块，冻结主干和 LoRA）。
    *   第二阶段训练 3 个 Epoch（联合训练 CTE 和 LoRA）。
    *   学习率在 $5 \times 10^{-6}$ 到 $1 \times 10^{-5}$ 之间。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 3 个主流数据集上进行了测试，涵盖了从 4B 到 8B 参数量的多种模型架构。
*   **消融实验**：针对“瓶颈训练策略的有效性”、“提示标记的设计（对比随机标记）”以及“提示标记数量 $K$ 的影响”进行了详细消融。
*   **充分性评价**：实验设计较为全面，通过跨模型、跨数据集的对比验证了框架的通用性。引入 LLM-as-a-judge 提高了开放式问题评估的客观性，定性分析（可视化）直观展示了模型预测的改进。

#### 6. 主要结论与发现
*   **性能提升**：InViC 在所有基准测试中均优于零样本推理和标准的 LoRA 微调。例如，在 SLAKE 数据集上，将 Qwen3-VL-4B 的准确率从 0.592 提升至 0.849。
*   **瓶颈效应**：第一阶段的瓶颈训练至关重要，它能有效“纠正”模型忽视视觉信息的倾向；若跳过此阶段，性能会显著下降。
*   **参数敏感性**：提示标记数量 $K=16$ 时效果最佳，过少会导致信息丢失，过多则引入冗余。

#### 7. 优点
*   **即插即用**：作为轻量级插件，无需改变原始 MLLM 的核心架构。
*   **机制创新**：通过“瓶颈掩码”从架构层面强制执行视觉对齐，而非仅仅依赖损失函数。
*   **意图对齐**：CTE 模块使视觉特征提取具有高度的问题相关性，减少了无关视觉噪声的干扰。

#### 8. 不足与局限
*   **计算开销**：虽然是轻量级的，但 CTE 模块增加了额外的交叉注意力计算。
*   **评估偏差**：尽管使用了 GPT-4o-mini 评分，但自动评估指标在医学专业领域的准确性仍可能与人类专家存在偏差。
*   **覆盖范围**：实验主要集中在放射学影像，对于病理学、内窥镜等其他医学影像领域的泛化能力尚待进一步验证。

（完）
