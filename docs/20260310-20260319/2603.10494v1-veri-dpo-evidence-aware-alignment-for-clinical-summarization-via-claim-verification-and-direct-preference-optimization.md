---
title: "VERI-DPO: Evidence-Aware Alignment for Clinical Summarization via Claim Verification and Direct Preference Optimization"
title_zh: VERI-DPO：通过论断验证与直接偏好优化实现临床摘要的证据感知对齐
authors: "Weixin Liu, Congning Ni, Qingyuan Song, Susannah L. Rose, Christopher Symons, Murat Kantarcioglu, Bradley A. Malin, Zhijun Yin"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10494v1"
tags: ["query:medllm"]
score: 9.0
evidence: 基于大语言模型的电子健康档案临床摘要生成
tldr: 针对临床总结中大模型易产生虚假陈述或过度简化的问题，本文提出 VERI-DPO 框架。该方法利用检索增强的验证器对生成的声明进行证据核查，通过挖掘具有证据支持且覆盖率高的偏好对，并结合直接偏好优化（DPO）进行对齐。实验表明，该方法在显著降低临床总结中不支持声明比例的同时，保持了信息的完整性和有效性。
selection_source: fresh_fetch
motivation: 现有的临床总结模型常出现不支持的虚假陈述，且简单的对齐策略容易导致模型为了规避错误而过度减少输出信息。
method: 提出 VERI-DPO 框架，利用检索增强验证器对候选摘要进行声明级核查，并基于矛盾密度和覆盖率挖掘偏好对进行 DPO 训练。
result: "在 MIMIC-III 数据集上，该方法将不支持声明的比例从 10.7% 降至 1.9%，同时将有效性从 76.7% 提升至 82.5%。"
conclusion: VERI-DPO 证明了通过细粒度的证据核查和偏好优化，可以有效提升临床总结的忠实度和实用性，并避免模型退化。
---

## 摘要
简要住院病程 (BHC) 叙述必须具备临床实用性，同时忠实于碎片化的电子健康记录 (EHR) 证据。基于大语言模型 (LLM) 的临床摘要生成器仍会引入无证据支持的陈述，而对齐过程可能会诱发遗漏（即“少说”退化现象）。我们提出了 VERI-DPO，它利用论断验证来挖掘偏好，并通过直接偏好优化 (DPO) 将其蒸馏至摘要生成器中。在 MIMIC-III-Ext-VeriFact-BHC 数据集（包含 100 名 ICU 患者；按患者级别划分）上，我们训练了一个检索增强验证器，通过单标记格式将“论断-证据”对标记为“支持”、“不支持”或“未提及”。该验证器对采样的 BHC 候选文本中的句子级论断进行评分，并将分值差异聚合为覆盖感知效用，从而挖掘长度受控且以矛盾为锚点的偏好对。在留出集患者上的实验表明，验证器挖掘的偏好能根据矛盾密度区分候选文本；VERI-DPO 将“不支持”论断的比例从 10.7% 降低至 1.9%（本地验证器评判）以及从 11.6% 降低至 6.4%（GPT-4o 评判），同时将有效性从 76.7% 提升至 82.5%，并保持了信息丰富的长度。

## Abstract
Brief Hospital Course (BHC) narratives must be clinically useful yet faithful to fragmented EHR evidence. LLM-based clinical summarizers still introduce unsupported statements, and alignment can encourage omissions ("say-less" degeneration). We introduce VERI-DPO, which uses claim verification to mine preferences and distill them into the summarizer with Direct Preference Optimization (DPO). On MIMIC-III-Ext-VeriFact-BHC (100 ICU patients; patient-level splits), we train a retrieval-augmented verifier to label claim-evidence pairs as Supported, Not Supported, or Not Addressed via a single-token format. The verifier scores sentence-level claims from sampled BHC candidates and aggregates margins into a coverage-aware utility to mine length-controlled, contradiction-anchored preference pairs. On held-out patients, verifier-mined preferences separate candidates by contradiction density, and VERI-DPO reduces Not Supported claim rates from 10.7% to 1.9% (local verifier judge) and from 11.6% to 6.4% (GPT-4o judge), while improving validity from 76.7% to 82.5% and maintaining informative length.

---

## 论文详细总结（自动生成）

### 论文总结：VERI-DPO 临床摘要证据感知对齐框架

#### 1. 核心问题与整体含义
临床摘要中的“简要住院病程”（BHC）要求极高的忠实度，必须严格基于电子健康档案（EHR）证据。然而，现有大语言模型（LLM）在生成此类摘要时常面临两大挑战：
*   **事实性错误（幻觉）**：生成未经病历支持的虚假陈述。
*   **“少说”退化（Degeneration-by-omission）**：在进行安全性或事实性对齐时，模型为了规避错误风险，倾向于生成极短、含糊或信息量贫乏的文本。
本文提出 **VERI-DPO** 框架，旨在通过细粒度的论断验证（Claim Verification）来挖掘高质量偏好数据，并利用直接偏好优化（DPO）实现证据感知的模型对齐，在降低幻觉的同时保持摘要的信息丰富度。

#### 2. 方法论
VERI-DPO 包含三个核心阶段：
*   **阶段 A：检索增强验证器训练**
    *   训练一个轻量级（8B级）验证器，输入为“检索到的证据片段 + 待核查论断”。
    *   采用单标记（Single-token）分类格式，输出 A（支持）、B（不支持/矛盾）、C（未提及）。
    *   引入逻辑偏置（Logit Bias）校准技术，在“不支持”类别的精确率和召回率之间取得平衡。
*   **阶段 B：验证器驱动的偏好挖掘**
    *   对同一提示词采样多个 BHC 候选文本，将其分解为句子级论断。
    *   利用验证器对每个论断打分，并聚合为摘要级的**效用函数 $U$**。
    *   **效用函数设计**：$U = \lambda_A n_A - \lambda_B n_B - \lambda_C n_C + \lambda_{cov} \text{Coverage} - \dots$（惩罚矛盾项 $B$，奖励支持项 $A$ 和覆盖率）。
    *   筛选偏好对：选择“不支持”论断少且满足长度/覆盖率约束的作为“优（Chosen）”，包含高置信度矛盾论断的作为“劣（Rejected）”。
*   **阶段 C：DPO 对齐**
    *   将挖掘出的偏好对用于 DPO 训练，使摘要模型内化验证器的判断标准，从而在单次推理中生成更忠实的摘要，无需推理时重排序。

#### 3. 实验设计
*   **数据集**：使用 **MIMIC-III-Ext-VeriFact-BHC**，包含 100 名 ICU 患者的病历及 13,070 个经过临床医生标注的命题。
*   **Benchmark 与对比方法**：
    *   **Base 模型**：Llama-3.1-8B-Instruct。
    *   **SFT Baseline**：仅在偏好对的“优”选文本上进行监督微调。
    *   **Verifier Rerank (Best-of-8)**：采样 8 个结果并用验证器选出最优者（计算开销大）。
*   **评估指标**：不支持论断率（NS-rate）、支持论断数（#Supp）、有效性（Valid %）、输出长度（Chars）。
*   **裁判系统**：采用本地验证器（Med42-8B）和外部裁判（GPT-4o）双重评估。

#### 4. 资源与算力
*   **模型规格**：主要基于 8B 参数规模的模型（Llama-3.1-8B, Med42-8B）。
*   **训练技术**：使用了 4-bit 量化（NF4）、LoRA 适配器（秩 $r=16$）、BF16 精度、梯度检查点（Gradient Checkpointing）。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量及总训练时长。仅提到批大小（Batch Size）为 1，梯度累积步数为 8。

#### 5. 实验数量与充分性
*   **实验规模**：在 100 名患者的数据上进行了患者级别的划分（72 训练 / 8 开发 / 20 测试）。
*   **消融实验**：针对偏好挖掘策略进行了消融研究，对比了随机配对、无高置信度锚点、无长度/覆盖率激励等设置。
*   **充分性评价**：实验设计较为严谨，通过双裁判机制降低了单一模型评估的偏见。虽然患者样本量（100人）相对较小，但由于临床标注极其昂贵且细致（命题级），该规模在临床 NLP 领域具有一定的代表性。

#### 6. 主要结论与发现
*   **显著降低幻觉**：在本地裁判下，不支持论断率从 10.7% 降至 **1.9%**；在 GPT-4o 裁判下从 11.6% 降至 **6.4%**。
*   **克服退化**：与 SFT 不同，DPO 不仅降低了错误率，还提升了摘要的有效性（76.7% -> 82.5%）和平均长度，证明模型没有通过“少说话”来逃避错误。
*   **优于重排序**：DPO 作为单次采样策略，其表现优于需要多次采样的 Best-of-8 重排序基准。

#### 7. 优点
*   **证据感知**：将细粒度的证据核查直接引入对齐流程，而非仅作为事后评估。
*   **可审计性**：验证器生成的中间产物（论断标签、证据 ID）为临床医生提供了可解释的审核线索。
*   **抗退化机制**：通过效用函数中的覆盖率和长度约束，有效解决了对齐中常见的模型“变懒”问题。

#### 8. 不足与局限
*   **数据局限性**：仅基于单一中心（MIMIC-III）的 ICU 数据，跨机构、跨科室的泛化能力有待验证。
*   **检索依赖性**：验证器的准确性高度依赖于检索组件的性能，若检索不到关键证据，会导致误判。
*   **算力说明缺失**：未提供具体的硬件环境和训练耗时，不利于复现时的资源估算。
*   **自动化偏见**：虽然使用了双裁判，但最终仍依赖 LLM 评估，可能存在与人类临床医生判断不一致的风险。

（完）
