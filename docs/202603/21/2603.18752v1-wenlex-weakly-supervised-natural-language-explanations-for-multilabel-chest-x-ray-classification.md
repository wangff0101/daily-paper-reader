---
title: "WeNLEX: Weakly Supervised Natural Language Explanations for Multilabel Chest X-ray Classification"
title_zh: WeNLEX：用于多标签胸部 X 射线分类的弱监督自然语言解释
authors: "Isabel Rio-Torto, Jaime S. Cardoso, Luís F. Teixeira"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18752v1"
tags: ["query:medllm"]
score: 8.0
evidence: 胸部X射线分类的自然语言解释
tldr: "WeNLEX是一个弱监督模型，旨在为多标签胸部X射线分类生成自然语言解释。它通过在特征空间中匹配生成图像与原始图像来确保解释的忠实性，并利用极少量临床标注数据保证解释的合理性。该模型不仅能生成忠实且多样化的解释，还能在联合训练时提升分类性能（AUC提升2.21%），并支持针对不同受众（如非医学用户）定制解释风格。"
selection_source: carryover_cache
motivation: 现有的解释生成方法过度依赖大量标注数据，且生成的解释往往只追求表面合理而缺乏对模型内部推理过程的忠实反映。
method: 提出一种弱监督框架，通过在特征空间对齐解释生成的图像与原图来保证忠实性，并结合分布对齐技术利用极少量样本确保解释的合理性。
result: "实验证明WeNLEX仅需每种诊断5条标注即可生成高质量解释，且在联合训练模式下将分类AUC提升了2.21%。"
conclusion: 该研究展示了将可解释性融入训练过程能有效提升下游任务性能，并证明了自然语言解释在不同受众群体中的灵活性和适应性。
---

## 摘要
自然语言解释提供了一种本质上易于人类理解的方式来解释黑盒模型，这与放射科医生在文本报告中传达诊断结果的方式密切相关。大多数研究使用带有解释标注的数据集来显式监督解释生成过程。因此，生成的解释虽然看似合理（plausible），但并不忠实（faithful）于模型的推理过程。在这项工作中，我们提出了 WeNLEX，这是一种用于生成多标签胸部 X 射线分类自然语言解释的弱监督模型。通过在黑盒模型的特征空间中，将从相应自然语言解释生成的图像与原始图像进行匹配，从而确保了忠实性。通过与临床医生标注的小型解释数据库进行分布对齐，来维持合理性。我们通过对评估忠实性、可模拟性、多样性和合理性的多项指标进行广泛验证，从实证上证明了 WeNLEX 能够生成忠实且合理的解释，且每种诊断仅需 5 条真实解释。此外，WeNLEX 可以在事后（post-hoc）和模型内（in-model）两种设置下运行。在后者中，即当多标签分类器与网络的其余部分共同训练时，WeNLEX 将独立分类器的分类 AUC 提高了 2.21%，从而表明在训练过程中加入可解释性实际上可以提高下游任务的性能。此外，只需更改数据库，WeNLEX 的解释即可适应任何目标受众；我们通过训练一个外行版本的 WeNLEX 展示了这种灵活性，其中解释针对非医学用户进行了简化。

## Abstract
Natural language explanations provide an inherently human-understandable way to explain black-box models, closely reflecting how radiologists convey their diagnoses in textual reports. Most works explicitly supervise the explanation generation process using datasets annotated with explanations. Thus, though plausible, the generated explanations are not faithful to the model's reasoning. In this work, we propose WeNLEX, a weakly supervised model for the generation of natural language explanations for multilabel chest X-ray classification. Faithfulness is ensured by matching images generated from their corresponding natural language explanations with original images, in the black-box model's feature space. Plausibility is maintained via distribution alignment with a small database of clinician-annotated explanations. We empirically demonstrate, through extensive validation on multiple metrics to assess faithfulness, simulatability, diversity, and plausibility, that WeNLEX is able to produce faithful and plausible explanations, using as little as 5 ground-truth explanations per diagnosis. Furthermore, WeNLEX can operate in both post-hoc and in-model settings. In the latter, i.e., when the multilabel classifier is trained together with the rest of the network, WeNLEX improves the classification AUC of the standalone classifier by 2.21%, thus showing that adding interpretability to the training process can actually increase the downstream task performance. Additionally, simply by changing the database, WeNLEX explanations are adaptable to any target audience, and we showcase this flexibility by training a layman version of WeNLEX, where explanations are simplified for non-medical users.

---

## 论文详细总结（自动生成）

以下是对论文《WeNLEX: Weakly Supervised Natural Language Explanations for Multilabel Chest X-ray Classification》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的医疗影像自然语言解释（NLE）生成模型大多依赖于“一对一”的全监督标注（即每张图像对应一个诊断解释）。这导致了两个主要问题：
    1.  **标注成本极高**：在医疗领域获取大规模高质量的放射科报告解释非常困难。
    2.  **忠实性（Faithfulness）缺失**：全监督模型倾向于模仿人类标注的文本（追求“合理性”），而非反映黑盒模型真实的决策逻辑。
*   **研究背景**：在医疗决策等高风险领域，解释必须不仅听起来合理（Plausible），更要真实反映模型的推理过程。

### 2. 方法论核心思想与技术细节
WeNLEX 采用**弱监督框架**，其核心思想是通过特征空间对齐和极少量样本的分布匹配来生成解释。
*   **架构设计**：采用“预测-解释”（Predict-Explain）范式。基于预训练的文本编码器-解码器，通过参数高效微调（PEFT）技术（如 LLaMA-Adapter）接入视觉特征。
*   **合理性（Plausibility）保障**：
    *   不要求图像与解释一一对应，而是建立一个小型诊断解释数据库（每种诊断仅需 5 条文本）。
    *   使用**最大均值差异（MMD）**或对抗学习（WGAN-GP）来最小化生成解释与数据库中真实解释之间的分布距离。
*   **忠实性（Faithfulness）保障**：
    *   **特征重构损失**：将生成的文本嵌入通过一个“文本转图像”模块生成伪图像，再将其输入待解释模型（MBE），要求伪图像在 MBE 特征空间中与原始图像特征匹配。
    *   **分类一致性损失**：要求从解释生成的伪图像在 MBE 中能得出与原图相同的诊断预测。
*   **灵活性**：支持**事后解释（Post-hoc）**和**模型内解释（In-model）**。在模型内设置下，分类器与解释生成器共同训练。

### 3. 实验设计
*   **数据集**：使用 **MIMIC-NLE**（从 MIMIC-CXR 报告中提取的 36,501 条解释和 28,548 张图像）。
*   **基准方法（Benchmark）**：对比了 Rio-Torto 等人 (2024) 提出的**全监督** NLE 生成模型。
*   **评估指标**：
    *   **合理性**：CXBS（基于 CheXbert 的 BERTScore）。
    *   **可模拟性**：使用 CheXagent 模型作为代理，测试仅凭解释（$\hat{y}|NLE$）或结合图像能否复现模型预测。
    *   **忠实性**：删除法（Deletion）——遮盖解释提及的区域看预测是否翻转（Flip %）及概率变化（$\Delta p$）；CLEV 评分。
    *   **多样性**：Self-BLEU 和基于检索的攻击测试。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号和数量。
*   **训练细节**：使用了 AdamW 优化器，训练 50 个 epoch，batch size 为 16，初始学习率为 $5 \times 10^{-4}$。文本自编码器在 Interpret-CXR 数据集上进行了预训练（50 万步）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对比了不同损失函数组合（MMD vs Adv、有无重构损失等）的效果。
    *   **消融实验**：针对特征重构应使用分类器的哪一层（从 DenseBlock1 到 Classifier 层）进行了详细对比；针对数据库样本量（2, 5, 10, 20 条）进行了鲁棒性测试。
    *   **受众适配实验**：对比了专业医疗版本和针对外行（Layman）的简化解释版本。
*   **充分性评价**：实验设计非常全面，不仅涵盖了传统的 NLP 指标，还引入了视觉接地（Grounding）和决策翻转等硬核忠实性指标，客观地证明了弱监督在减少数据依赖的同时提升了模型性能。

### 6. 主要结论与发现
*   **性能超越全监督**：WeNLEX 仅需每种诊断 5 条标注，在忠实性和多样性指标上即可超越全监督模型。
*   **解释促进分类**：在“模型内”训练模式下，加入解释生成任务使分类器的 **AUC 提升了 2.21%**，打破了“可解释性必然牺牲性能”的迷思。
*   **极低数据依赖**：实验发现每种诊断提供 5 条解释与提供 20 条解释的效果相近，证明了模型对极少量标注的强大利用能力。
*   **受众可定制**：通过简单更换解释数据库，模型能无缝切换解释风格（如将专业术语转化为通俗易懂的语言）。

### 7. 优点与亮点
*   **弱监督创新**：成功摆脱了昂贵的逐图标注依赖，对实际医疗落地具有重大意义。
*   **忠实性闭环**：通过“文本->图像->特征”的重构路径，从数学和逻辑上约束了解释必须与模型内部特征挂钩。
*   **多维度评估**：不局限于 BLEU 等文本相似度指标，而是从人类理解、模型决策一致性等多个维度定义了“什么是好的解释”。

### 8. 不足与局限
*   **代理模块风险**：模型依赖一个简单的“文本转图像”模块来传递梯度，该模块生成的图像质量及它能否准确承载复杂的病理特征可能存在瓶颈。
*   **评估依赖性**：忠实性和可模拟性的评估高度依赖于第三方模型（如 CheXagent 和 CheXbert），如果这些评估模型本身存在偏差，实验结果可能受影响。
*   **计算开销**：虽然标注成本降低，但训练过程中涉及图像生成和多次特征提取，计算复杂度高于传统的文本生成模型。

（完）
