---
title: Synergizing Discriminative Exemplars and Self-Refined Experience for MLLM-based In-Context Learning in Medical Diagnosis
title_zh: 协同判别性范例与自精炼经验，用于医学诊断中基于 MLLM 的上下文学习
authors: "Wenkai Zhao, Zipei Wang, Mengjie Fang, Di Dong, Jie Tian, Lingwei Zhang"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27737v1"
tags: ["query:medllm"]
score: 9.0
evidence: 基于多模态大模型的医学诊断上下文学习
tldr: 针对通用多模态大模型在医疗诊断中难以捕捉领域细节且微调成本高的问题，本文提出一种模拟临床医生工作流的上下文学习框架。该框架结合了判别性样本核心集选择（DECS）和自我改进经验总结（SRES），通过筛选视觉锚点案例和动态提炼诊断经验，在不更新模型参数的情况下显著提升了诊断性能，为参数高效的医疗AI应用提供了新思路。
selection_source: fresh_fetch
motivation: 通用多模态大模型在医疗诊断中表现不佳，且全量微调面临专家标注成本高和计算开销大的挑战。
method: 提出模拟临床医生工作流的ICL框架，结合判别性视觉样本筛选（DECS）与动态文本经验总结（SRES）来增强模型推理。
result: 在MedMNIST 2D基准的12个数据集上，该方法优于零样本模型，并达到了与全监督视觉模型及微调后模型相当的水平。
conclusion: 该研究证明了通过协同视觉样本与反思经验，可以在不更新参数的情况下实现高性能的医疗多模态上下文学习。
---

## 摘要
通用多模态大语言模型 (MLLMs) 在捕捉医学诊断中的领域特定细微差别方面往往表现不佳，落后于全监督基准。虽然微调提供了一种补救措施，但专家标注的高昂成本和巨大的计算开销限制了其可扩展性。为了在不更新 MLLM 预训练主干权重的情况下弥补这一差距，我们提出了一种临床医生模拟工作流 (Clinician Mimetic Workflow)。这是一种新型的上下文学习 (ICL) 框架，旨在协同判别性范例核心集选择 (DECS) 和自精炼经验总结 (SRES)。具体而言，DECS 通过在计算层面从噪声数据中选择判别性视觉核心集，模拟了临床医生参考“锚点病例”的能力；同时，SRES 通过将多样的推演过程提炼为动态的文本经验库，模仿了临床诊断中的认知与反思。在 MedMNIST 2D 基准的所有 12 个数据集上进行的广泛评估表明，我们的方法优于零样本通用和医学 MLLM。同时，它达到了与全监督视觉模型和领域特定微调 MLLM 相当的性能水平，为参数高效的医学上下文学习树立了新的基准。我们的代码可在匿名仓库获取：https://anonymous.4open.science/r/Synergizing-Discriminative-Exemplars-and-Self-Refined-Experience-ED74。

## Abstract
General Multimodal Large Language Models (MLLMs) often underperform in capturing domain-specific nuances in medical diagnosis, trailing behind fully supervised baselines. Although fine-tuning provides a remedy, the high costs of expert annotation and massive computational overhead limit its scalability. To bridge this gap without updating the weights of the pre-trained backbone of the MLLM, we propose a Clinician Mimetic Workflow. This is a novel In-Context Learning (ICL) framework designed to synergize Discriminative Exemplar Coreset Selection (DECS) and Self-Refined Experience Summarization (SRES). Specifically, DECS simulates a clinician's ability to reference "anchor cases" by selecting discriminative visual coresets from noisy data at the computational level; meanwhile, SRES mimics the cognition and reflection in clinical diagnosis by distilling diverse rollouts into a dynamic textual Experience Bank. Extensive evaluation across all 12 datasets of the MedMNIST 2D benchmark demonstrates that our method outperforms zero-shot general and medical MLLMs. Simultaneously, it achieves performance levels comparable to fully supervised vision models and domain-specific fine-tuned MLLMs, setting a new benchmark for parameter-efficient medical in-context learning. Our code is available at an anonymous repository: https://anonymous.4open.science/r/Synergizing-Discriminative-Exemplars-and-Self-Refined-Experience-ED74.

---

## 论文详细总结（自动生成）

### 论文总结：协同判别性范例与自精炼经验的医学 MLLM 上下文学习框架

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：通用多模态大语言模型（MLLM）在特定医学诊断任务中，由于缺乏领域知识和对医学图像细微差别的捕捉能力，性能往往远逊于全监督的视觉模型。
*   **研究背景**：虽然微调（Fine-tuning）可以提升性能，但医学领域面临专家标注成本极高、计算资源消耗巨大的挑战。
*   **研究动机**：探索一种**无需更新模型参数**的上下文学习（In-Context Learning, ICL）方案，通过模拟临床医生的两种核心认知能力——“参考既往病例（锚点病例）”和“反思总结诊断经验”，来增强冻结主干 MLLM 的诊断精度。

#### 2. 方法论：临床医生模拟工作流（Clinician Mimetic Workflow）
该框架由两个协同的核心组件组成，旨在为冻结的 MLLM 提供视觉结构先验和文本逻辑先验：
*   **判别性范例核心集选择（DECS）**：
    *   **核心思想**：模拟医生寻找典型病例的过程，从含有噪声的训练集中筛选出最具代表性的视觉样本。
    *   **技术细节**：使用视觉编码器提取特征，通过**硬样本挖掘策略**（寻找同类中相似度最低的样本）和**指数移动平均（EMA）更新**，在特征空间中优化类原型。这种方法能有效过滤掉对比度、染色强度等无关噪声，强化诊断相关的生物标志物特征。
*   **自精炼经验总结（SRES）**：
    *   **核心思想**：模拟医生的诊断反思与规则更新。
    *   **技术细节**：
        1.  **奖励引导推演**：针对查询图像生成多个推理路径（Rollouts）。
        2.  **评估与对比**：由 LLM 充当评估者，根据正确性、一致性等临床标准对路径打分。
        3.  **经验提炼**：对比高分和低分路径的语义差异，总结出诊断启发式规则（Heuristics），并存入动态更新的“经验库（Experience Bank）”。
        4.  **动作策略**：通过 Add（增加）、Delete（删除）、Modify（修改）等操作维护经验库的质量和容量。

#### 3. 实验设计
*   **数据集**：采用了 **MedMNIST 2D** 基准测试中的全部 **12 个数据集**，涵盖了 X射线、CT、超声、皮肤镜、病理切片等多种模态，涉及二分类、多分类及多标签分类任务。
*   **Benchmark**：以 MedMNIST 官方提供的全监督基准为主要参照。
*   **对比方法**：
    *   **全监督视觉模型**：ResNet50, ViT-B, Swin-B, ConvNeXt-B, SigLIP2-B。
    *   **通用 MLLM（零样本）**：GPT-4o-mini, Gemini-1.5-flash。
    *   **医学专用 MLLM**：LLaVA-Med, MedGemma。
    *   **微调后的医学 MLLM**：BiomedGPT, Hulu-Med。

#### 4. 资源与算力
*   **硬件环境**：实验在一台单张 **NVIDIA RTX 6000 GPU** 上完成。
*   **软件框架**：基于 PyTorch 和 vLLM 推理框架实现。
*   **模型主干**：使用 **Qwen2-VL-8B**（论文中注为 Qwen3-VL，可能为笔误或特定版本）作为冻结的 MLLM 主干。
*   **训练/优化时长**：文中未明确给出具体的总耗时，但强调了 DECS 的优化和 SRES 的经验库进化过程非常高效（SRES 仅需 200 个随机训练样本即可完成进化）。

#### 5. 实验数量与充分性
*   **实验规模**：在 12 个不同类型的医学数据集上进行了全面测试，每项任务均报告了多次运行的平均准确率（ACC）。
*   **消融实验**：详细对比了“无增强”、“仅 DECS”、“仅 SRES”、“随机样本 + SRES”以及“完整框架”的性能差异，验证了各组件的有效性。
*   **客观性**：对比了从全监督到零样本、从通用到专用的多种模型，实验设计较为全面、客观，能够反映出该方法在不同医学场景下的泛化能力。

#### 6. 主要结论与发现
*   **性能卓越**：该方法在不更新参数的情况下，平均准确率达到 **86.3%**，显著优于所有零样本通用和医学 MLLM（比 Gemini-1.5-flash 高出 29.2%）。
*   **媲美微调**：其表现达到了与全监督视觉模型及经过大量微调的医学专用 MLLM（如 BiomedGPT）相近的水平。
*   **协同效应**：实验证明视觉范例（DECS）和文本经验（SRES）的结合具有 1+1>2 的效果，缺一不可。

#### 7. 优点（亮点）
*   **参数高效性**：完全不需要对大模型进行微调，极大地降低了部署成本和对专家标注数据的依赖。
*   **认知模拟**：将临床医生的“病例参考”与“反思总结”逻辑转化为可计算的算法流程，具有较强的可解释性。
*   **鲁棒性**：DECS 通过特征空间优化有效解决了医学图像中常见的噪声和类内差异问题。

#### 8. 不足与局限
*   **多标签分类瓶颈**：在 ChestMNIST（多标签任务）上提升有限。原因是有限的上下文窗口难以覆盖 14 种病理的组合证据，且 SRES 的奖励机制在类别严重失衡时会产生偏见。
*   **推理开销**：虽然不需要训练，但在推理时需要检索范例并拼接长文本经验，可能会增加单次推理的 Token 消耗和延迟。
*   **评估指标单一**：主要依赖准确率（ACC），由于 MLLM 的生成特性，难以像判别模型那样给出稳定的 AUC 曲线。

（完）
