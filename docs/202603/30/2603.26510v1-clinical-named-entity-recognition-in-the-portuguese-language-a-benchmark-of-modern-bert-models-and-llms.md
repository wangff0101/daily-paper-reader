---
title: "Clinical named entity recognition in the Portuguese language: a benchmark of modern BERT models and LLMs"
title_zh: 葡萄牙语临床命名实体识别：现代 BERT 模型与大语言模型（LLMs）的基准测试
authors: "Vinicius Anjos de Almeida, Sandro Saorin da Silva, Josimar Chire, Leonardo Vicenzi, Nícolas Henrique Borges, Helena Kociolek, Sarah Miriã de Castro Rocha, Frederico Nassif Gomes, Júlia Cristina Ferreira, Oge Marques, Lucas Emanuel Silva e Oliveira"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26510v1"
tags: ["query:medllm"]
score: 9.0
evidence: 评估大模型在葡萄牙语临床笔记命名实体识别中的应用
tldr: 本研究针对葡萄牙语临床命名实体识别（NER）基准匮乏的问题，对比了BioBERTpt、BERTimbau、ModernBERT、mmBERT等BERT模型与GPT-5、Gemini-2.5等大语言模型。通过在SemClinBr和私有乳腺癌数据集上的实验，并结合迭代分层等策略解决类别不平衡问题，发现mmBERT表现最优，为本地化部署临床信息提取提供了高效方案。
selection_source: fresh_fetch
motivation: 旨在填补葡萄牙语临床命名实体识别基准的空白，并探索解决多标签类别不平衡的策略。
method: 对比了多种BERT变体与主流大语言模型，并应用迭代分层、加权损失和过采样等技术优化模型训练。
result: mmBERT-base模型以0.76的微F1分数脱颖而出，性能优于其他BERT模型及大语言模型。
conclusion: 多语言BERT模型（尤其是mmBERT）在处理葡萄牙语临床NER任务时表现强劲，且支持低资源本地运行。
---

## 摘要
临床笔记包含宝贵的非结构化信息。命名实体识别（NER）能够自动提取医学概念；然而，针对葡萄牙语的基准测试仍然稀缺。在本研究中，我们旨在评估基于 BERT 的模型和大语言模型（LLMs）在葡萄牙语临床 NER 中的表现，并测试解决多标签不平衡问题的策略。我们使用公开的 SemClinBr 语料库和私有乳腺癌数据集，对比了 BioBERTpt、BERTimbau、ModernBERT 和 mmBERT 与 GPT-5 及 Gemini-2.5 等大语言模型。模型在相同条件下进行训练，并使用精确率、召回率和 F1 分数进行评估。研究探索了迭代分层、加权损失和过采样等方法以缓解类别不平衡。mmBERT-base 模型取得了最佳性能（微 F1 = 0.76），优于所有其他模型。迭代分层改善了类别平衡和整体性能。多语言 BERT 模型（特别是 mmBERT）在葡萄牙语临床 NER 中表现强劲，且可以在有限的计算资源下本地运行。平衡的数据拆分策略进一步提升了性能。

## Abstract
Clinical notes contain valuable unstructured information. Named entity recognition (NER) enables the automatic extraction of medical concepts; however, benchmarks for Portuguese remain scarce. In this study, we aimed to evaluate BERT-based models and large language models (LLMs) for clinical NER in Portuguese and to test strategies for addressing multilabel imbalance. We compared BioBERTpt, BERTimbau, ModernBERT, and mmBERT with LLMs such as GPT-5 and Gemini-2.5, using the public SemClinBr corpus and a private breast cancer dataset. Models were trained under identical conditions and evaluated using precision, recall, and F1-score. Iterative stratification, weighted loss, and oversampling were explored to mitigate class imbalance. The mmBERT-base model achieved the best performance (micro F1 = 0.76), outperforming all other models. Iterative stratification improved class balance and overall performance. Multilingual BERT models, particularly mmBERT, perform strongly for Portuguese clinical NER and can run locally with limited computational resources. Balanced data-splitting strategies further enhance performance.

---

## 论文详细总结（自动生成）

这篇论文对葡萄牙语临床命名实体识别（NER）任务进行了深入的基准测试，对比了现代 BERT 模型与大语言模型（LLMs）的表现。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：临床笔记中蕴含大量非结构化医疗信息，NER 是自动提取这些概念的关键技术。然而，针对葡萄牙语临床领域的 NER 基准测试非常匮乏。
*   **核心问题**：评估现代编码器模型（如 BERT 变体）与生成式大模型（如 GPT-5、Gemini-2.5）在处理葡萄牙语医疗文本时的优劣，并探索解决医疗数据中严重的类别不平衡（Class Imbalance）问题的有效策略。

### 2. 方法论
*   **核心思想**：将 NER 建模为多标签标记分类（Multilabel Token Classification）任务，通过对比不同架构的模型和数据处理策略来优化提取效果。
*   **关键技术细节**：
    *   **模型选择**：对比了专门针对葡萄牙语优化的模型（BioBERTpt、BERTimbau）和最新的多语言/高效架构模型（mmBERT、ModernBERT）。
    *   **数据拆分策略**：引入**迭代分层（Iterative Stratification）**算法，确保在多标签场景下，稀有类别在训练、验证和测试集中的分布比例一致。
    *   **不平衡处理**：测试了加权损失函数（Weighted Loss，根据类别频率倒数计算权重）、权重截断（Clamped Weights，固定最小权重为 1.0）以及过采样（Oversampling）技术。
    *   **标注方案**：对比了 IO（Inside-Outside）和 BIO（Beginning-Inside-Outside）两种标注模式。
    *   **LLM 策略**：采用 Few-shot 提示词工程，并测试了 GPT-5 的不同推理努力程度（Reasoning Effort）。

### 3. 实验设计
*   **数据集**：
    1.  **SemClinBr**：公开数据集，包含 1,000 条临床记录，涵盖 12 个 UMLS 语义组。
    2.  **乳腺癌私有数据集**：来自巴西某肿瘤中心的 500 条门诊笔记，由专业团队标注了 10 类特定实体（如 HER2、BRCA、手术方式等）。
*   **对比方法**：
    *   **BERT 类**：BioBERTpt (all/clin/bio), BERTimbau (base/large), ModernBERT (base/large), mmBERT (base/small)。
    *   **LLM 类**：GPT-4.1, GPT-5 (nano/mini/standard), Gemini-2.5 (flash/pro)。
*   **Benchmark 指标**：精确率（Precision）、召回率（Recall）和 F1 分数（Micro/Macro）。

### 4. 资源与算力
*   **硬件环境**：使用单台桌面工作站，配置为 Intel i9-13900K CPU，64GB DDR5 RAM，以及一块 **NVIDIA GeForce RTX 4090 (24GB VRAM)** GPU。
*   **训练细节**：学习率 5e-5，Batch Size 为 10，梯度累积为 5，最大输入长度 512 标记。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了约 10 种 BERT 变体和 10 种 LLM 配置。针对类别不平衡处理，每个实验重复运行了 10 次以获取均值和标准差。
*   **充分性评价**：实验设计较为全面，不仅涵盖了公开与私有数据集，还深入探讨了标注方案（IO vs BIO）和数据拆分方法对结果的影响。通过对比不同推理强度的 GPT-5，客观展示了成本、时间与性能之间的权衡。

### 6. 主要结论与发现
*   **mmBERT 表现最优**：mmBERT-base 在 SemClinBr 数据集上达到了 **0.7646 的微 F1 分数**，创下了新的 SOTA（先进水平），优于所有 LLM。
*   **BERT 优于 LLM**：在特定 NER 任务中，经过微调的 BERT 模型性能普遍高于提示词驱动的 LLM，且推理速度更快、成本更低、隐私安全性更高（可本地运行）。
*   **数据策略至关重要**：使用迭代分层（IS）替代随机拆分，使 mmBERT 的 F1 分数从 0.69 提升至 0.76。
*   **LLM 的局限性**：GPT-5 增加推理努力程度虽能略微提升性能，但会导致成本和推理时间大幅增加，且仍无法超越最佳的 BERT 模型。

### 7. 优点
*   **实战价值高**：在私有肿瘤数据集上的成功验证，证明了该方案在真实医疗机构中部署的可行性。
*   **方法论严谨**：系统性地量化了迭代分层和加权损失对不平衡数据的改进效果。
*   **开源贡献**：提供了完整的源代码，促进了葡萄牙语临床 NLP 社区的发展。

### 8. 不足与局限
*   **数据污染风险**：mmBERT 的预训练数据量巨大，不能完全排除其在预训练阶段见过公开数据集 SemClinBr 的可能性。
*   **LLM 优化不足**：未对 LLM 进行微调（Fine-tuning），仅使用了提示词工程，可能未激发 LLM 的全部潜力。
*   **覆盖范围**：虽然涵盖了多个科室，但医疗领域的复杂性极高，结果在其他特定专科或地区的泛化性仍需进一步验证。
*   **统计检验**：在处理类别不平衡的实验中，缺乏严格的假设检验来进一步证实不同技术间的显著性差异。

（完）
