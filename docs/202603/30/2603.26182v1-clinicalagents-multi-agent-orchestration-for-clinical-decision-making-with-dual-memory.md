---
title: "ClinicalAgents: Multi-Agent Orchestration for Clinical Decision Making with Dual-Memory"
title_zh: ClinicalAgents：基于双记忆机制的临床决策多智能体编排
authors: "Zhuohan Ge, Haoyang Li, Yubo Wang, Nicole Hu, Chen Jason Zhang, Qing Li"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26182v1"
tags: ["query:medllm"]
score: 10.0
evidence: 用于临床诊断和决策的多智能体编排
tldr: 针对大语言模型在复杂临床诊断中推理能力不足的问题，本文提出ClinicalAgents多智能体框架。该框架通过蒙特卡洛树搜索（MCTS）模拟临床专家的动态决策过程，并结合双重记忆机制（动态工作记忆与静态经验记忆）进行假设验证与回溯。实验证明，该方法在诊断准确性和可解释性上均达到SOTA水平，有效弥补了传统线性推理的局限。
selection_source: fresh_fetch
motivation: 现有大模型在临床诊断中多采用静态线性推理，难以模拟人类医生复杂的假设驱动和迭代推理过程。
method: 提出一种基于MCTS动态编排的多智能体框架，并结合双重记忆架构来维护患者状态和检索临床指南。
result: 实验结果显示，ClinicalAgents在多个基准测试中显著优于现有的单智能体和多智能体方法。
conclusion: 该框架通过模拟专家的认知工作流，显著提升了临床决策的准确性与逻辑透明度。
---

## 摘要
尽管大语言模型（LLMs）在医疗保健领域展现出潜力，但它们在处理准确临床诊断所需的复杂非线性推理时往往面临困难。现有方法通常依赖于从症状到诊断的静态线性映射，未能捕捉到人类临床医生固有的迭代式、假设驱动的推理过程。为了弥补这一差距，我们引入了 ClinicalAgents，这是一种旨在模拟专家级临床医生认知工作流的新型多智能体框架。与僵化的顺序链不同，ClinicalAgents 采用了一种建模为蒙特卡洛树搜索（MCTS）过程的动态编排机制。这使得编排器（Orchestrator）能够迭代地生成假设、主动验证证据，并在关键信息缺失时触发回溯。该框架的核心是双记忆（Dual-Memory）架构：一个用于维护不断演变的患者状态以进行上下文感知推理的可变工作记忆（Working Memory），以及一个通过主动反馈循环检索临床指南和历史病例的静态经验记忆（Experience Memory）。大量实验表明，ClinicalAgents 达到了最先进的性能，与强大的单智能体和多智能体基准模型相比，显著提高了诊断准确性和可解释性。

## Abstract
While Large Language Models (LLMs) have demonstrated potential in healthcare, they often struggle with the complex, non-linear reasoning required for accurate clinical diagnosis. Existing methods typically rely on static, linear mappings from symptoms to diagnoses, failing to capture the iterative, hypothesis-driven reasoning inherent to human clinicians. To bridge this gap, we introduce ClinicalAgents, a novel multi-agent framework designed to simulate the cognitive workflow of expert clinicians. Unlike rigid sequential chains, ClinicalAgents employs a dynamic orchestration mechanism modeled as a Monte Carlo Tree Search (MCTS) process. This allows an Orchestrator to iteratively generate hypotheses, actively verify evidence, and trigger backtracking when critical information is missing. Central to this framework is a Dual-Memory architecture: a mutable Working Memory that maintains the evolving patient state for context-aware reasoning, and a static Experience Memory that retrieves clinical guidelines and historical cases via an active feedback loop. Extensive experiments demonstrate that ClinicalAgents achieves state-of-the-art performance, significantly enhancing both diagnostic accuracy and explainability compared to strong single-agent and multi-agent baselines.

---

## 论文详细总结（自动生成）

以下是对论文《ClinicalAgents: Multi-Agent Orchestration for Clinical Decision Making with Dual-Memory》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：临床决策（CDM）是一个复杂的、假设驱动的迭代过程，医生需要根据症状不断提出假设、收集证据并修正诊断。
*   **核心问题**：现有的大语言模型（LLM）和多智能体系统（MAS）在临床诊断中存在三大局限：
    1.  **推理线性化**：多采用静态、预设的顺序工作流，缺乏灵活的回溯和重新规划能力。
    2.  **记忆碎片化**：依赖窗口有限的对话上下文，容易丢失关键临床细节，导致诊断不一致。
    3.  **缺乏主动验证**：往往将诊断视为静态生成任务，无法在证据不足时主动识别并请求缺失信息。
*   **整体含义**：本文提出了 **ClinicalAgents** 框架，旨在通过模拟专家的认知流（感知-假设-验证-更新）来提升临床决策的准确性和可靠性。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将临床诊断建模为一个基于**蒙特卡洛树搜索（MCTS）**的动态编排过程，并辅以**双重记忆架构**。
*   **关键技术细节**：
    *   **临床编排器（Clinical Orchestrator）**：将智能体协作建模为马尔可夫决策过程（MDP）。利用 MCTS 进行路径搜索，动作空间包括调用特定专家智能体、检索知识、执行回溯（Backtracking）或终止诊断。
    *   **奖励函数（Reward Function）**：基于信息增益设计，当行动能减少缺失证据数量或提高诊断假设的置信度时，给予正向奖励。
    *   **证据触发的回溯机制**：当验证步骤发现关键证据缺失时，编排器会触发回溯，引导系统返回信息收集阶段，主动查询缺失的检查结果或症状。
    *   **双重记忆系统（Dual-Memory）**：
        *   **工作记忆（Working Memory）**：动态维护当前的证据集、假设集和行动轨迹，确保各智能体状态同步。
        *   **经验记忆（Experience Memory）**：包含静态临床指南和历史病例。利用图增强检索（AGRAG）获取权威指南，并从历史病例中提取“因果诊断链（CDC）”来预测潜在的缺失证据。

### 3. 实验设计
*   **数据集与场景**：
    *   **MedChain 基準测试**：包含 12,163 个样本，涵盖分诊、检查开立、影像解读、诊断、治疗五个连续阶段。
    *   **通用医疗 QA**：MedQA、PubMedQA、MedBullets、PathVQA（用于验证泛化能力）。
*   **对比方法**：
    *   **基础模型**：GPT-5.2、DeepSeek-R1、Qwen3-VL、Llama-4 等。
    *   **单智能体范式**：Few-shot+CoT、RAG、ReAct。
    *   **多智能体框架**：AutoGen、MedAgents、MDAgents、ColaCare、MedChain-Agents 等。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或训练总时长。
*   **实现细节**：实验主要基于 API 调用（如 GPT-5.2）和开源模型（如 Qwen3-VL-235B、DeepSeek-R1）进行推理评估。最大编排步数设置为 4，并启用了提前停止机制。

### 5. 实验数量与充分性
*   **实验规模**：在 MedChain 的影像子集（2,023 个案例）上进行了核心实验，并在 4 个标准 QA 数据集上进行了泛化测试。
*   **充分性评价**：
    *   **消融实验**：分别验证了双记忆系统和 MCTS 编排器的贡献，证明了各模块的互补性。
    *   **敏感性分析**：测试了不同底层模型（Backbone）对框架性能的影响，证明了框架的鲁棒性。
    *   **客观性**：使用了统一的 Backbone 进行对比，并采用了 LLM-as-a-judge 和 IoU 等多种客观指标，实验设计较为公平、充分。

### 6. 主要结论与发现
*   **性能领先**：ClinicalAgents 在 MedChain 任务中平均得分 0.5107，显著优于 GPT-5.2（0.4521）和最强的多智能体基准 MedChain-Agents（0.4880）。
*   **回溯的价值**：在“检查开立（Test Ordering）”任务中提升最明显（+24.5%），证明了回溯机制在完善证据链方面的有效性。
*   **稳定性**：双记忆机制有效减少了多轮对话中的信息丢失，使诊断过程更具逻辑一致性。
*   **泛化性**：在非工作流式的通用医疗问答任务中，该框架依然保持了最优性能。

### 7. 优点与亮点
*   **认知模拟**：成功将临床医生的“假设-演绎”推理逻辑转化为可计算的 MCTS 搜索过程。
*   **动态性**：突破了传统 MAS 的僵化顺序，支持根据证据情况动态调整诊断路径。
*   **知识集成**：通过 CDC（因果诊断链）将历史病例经验转化为主动的证据发现能力，具有很强的临床实用价值。

### 8. 不足与局限
*   **视觉能力依赖**：在影像解读任务中，性能高度受限于底层视觉语言模型（VLM）的原始能力，框架本身的编排提升有限。
*   **计算开销**：基于 MCTS 的多次模拟和回溯会显著增加推理延迟和 API 调用成本。
*   **伦理与偏差**：尽管有指南约束，但底层 LLM 固有的偏见和幻觉风险在复杂诊断中仍需人工最终把关。
*   **部署复杂性**：多智能体协作与双记忆系统的维护对实际医疗系统的集成提出了较高要求。

（完）
