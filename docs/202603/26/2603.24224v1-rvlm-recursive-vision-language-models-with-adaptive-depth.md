---
title: "RVLM: Recursive Vision-Language Models with Adaptive Depth"
title_zh: RVLM：具有自适应深度的递归视觉语言模型
authors: "Nicanor Mayumu, Zeenath Khan, Melodena Stephens, Patrick Mukala, Farhad Oroumchian"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24224v1"
tags: ["query:medllm"]
score: 9.0
evidence: 用于可审计医疗诊断声明的递归视觉语言模型
tldr: 针对医疗AI中视觉语言模型推理过程不透明及迭代推理计算资源分配不均的问题，本文提出RVLM框架。该框架通过“生成-执行”循环将推理过程转化为可审计的Python代码，并引入RRouter实现自适应迭代深度。在脑部MRI和胸部X射线数据集上的实验表明，RVLM在无需微调的情况下，能显著提升诊断的一致性与可解释性，并有效识别跨模态差异，为临床AI治理提供了新方案。
selection_source: fresh_fetch
motivation: 传统医疗VLM存在推理过程不可审计的黑盒问题，且现有的迭代推理系统因固定迭代次数导致简单任务浪费计算资源而复杂任务深度不足。
method: 提出一种递归视觉语言模型框架，通过编写并执行Python代码驱动视觉子代理进行迭代推理，并利用RRouter控制器根据任务复杂度动态调整推理深度。
result: 在BraTS 2023和MIMIC-CXR数据集上，RVLM在无需微调的情况下实现了高一致性的诊断，并能准确识别复杂的影像特征和跨模态差异。
conclusion: RVLM通过将推理过程代码化和自适应化，为构建可审计、高效且高性能的临床医疗AI系统提供了一种有效的新范式。
---

## 摘要
医疗人工智能系统面临两个根本性的局限。首先，传统的视觉语言模型（VLM）执行单次推理，产生黑盒预测，无法在临床术语层面进行审计或解释。其次，展示中间步骤的迭代推理系统依赖于固定的迭代预算，在简单案例上浪费计算资源，而在复杂案例上深度不足。我们通过一个统一的框架解决了这两个局限。RVLM 用迭代的“生成-执行”循环取代了单次推理：在每一步中，模型编写 Python 代码，调用视觉子代理，处理图像并积累证据。每一个诊断结论都基于可执行代码，满足了临床 AI 治理框架的可审计性要求。RRouter 使迭代深度具备自适应性：一个轻量级控制器根据任务复杂度特征预测最佳预算，随后监控进度并在推理停滞时提前终止。我们在 BraTS 2023 脑膜瘤（脑部 MRI）和 MIMIC-CXR（胸部 X 光）数据集上，使用未经微调的 Gemini 2.5 Flash 进行了评估。在多次重复运行中，RVLM 在显著发现（如肿块存在和增强）上表现出高度一致性，并能检测出流体衰减反转恢复（FLAIR）信号特征与分割边界之间的跨模态差异。在 MIMIC-CXR 上，它能生成结构化报告并正确识别特定视图的伪影。代码：https://github.com/nican2018/rvlm。

## Abstract
Medical AI systems face two fundamental limitations. First, conventional vision-language models (VLMs) perform single-pass inference, yielding black-box predictions that cannot be audited or explained in clinical terms. Second, iterative reasoning systems that expose intermediate steps rely on fixed iteration budgets wasting compute on simple cases while providing insufficient depth for complex ones. We address both limitations with a unified framework. RVLM replaces single-pass inference with an iterative generate-execute loop: at each step, the model writes Python code, invokes vision sub-agents, manipulates images, and accumulates evidence. Every diagnostic claim is grounded in executable code, satisfying auditability requirements of clinical AI governance frameworks. RRouter makes iteration depth adaptive: a lightweight controller predicts the optimal budget from task-complexity features, then monitors progress and terminates early when reasoning stalls. We evaluate on BraTS 2023 Meningioma (brain MRI) and MIMIC-CXR (chest X-ray) using Gemini 2.5 Flash without fine-tuning. Across repeated runs, RVLM shows high consistency on salient findings (e.g., mass presence and enhancement) and can detect cross-modal discrepancies between Fluid-Attenuated Inversion Recovery (FLAIR) signal characteristics and segmentation boundaries. On MIMIC-CXR, it generates structured reports and correctly recognises view-specific artefacts. Code: https://github.com/nican2018/rvlm.

---

## 论文详细总结（自动生成）

### RVLM：具有自适应深度的递归视觉语言模型论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
医疗 AI 系统在临床应用中面临两大核心挑战：
*   **解释性与审计困境：** 传统的视觉语言模型（VLM）多采用“单次推理（Single-pass）”模式，直接输出诊断结果。这种“黑盒”预测缺乏中间推理步骤，无法满足临床医学对证据链的要求，也难以符合《欧盟 AI 法案》等监管框架对高风险 AI 系统透明度和可追溯性的强制要求。
*   **计算资源分配不均：** 现有的迭代推理系统通常设定固定的迭代次数（Fixed budget）。这导致简单病例（如边界清晰的肿瘤）浪费计算资源，而复杂病例（如多发性、浸润性病变）则因推理深度不足而影响诊断准确性。

#### 2. 方法论：核心思想与关键技术
论文提出了一个结合 **RVLM**（解释层）与 **RRouter**（效率层）的统一框架：
*   **RVLM（递归视觉语言模型）：** 
    *   **核心思想：** 将推理过程转化为“生成-执行”循环。模型在持久化的 Python REPL 环境中运行，通过编写代码调用视觉子代理。
    *   **关键技术：** 引入了视觉原语（如 `describe_image`、`llm_query_with_images`）和图像处理函数（裁剪、增强、差分图）。模型在迭代中积累证据，每一项诊断声明都对应一段可执行的代码。
    *   **报告生成：** 最终由次级 LLM 将代码执行轨迹转换为符合临床规范的结构化 PDF 报告。
*   **RRouter（递归路由）：**
    *   **自适应深度：** 提取图像特征（如标签熵 $H$、肿瘤体积 $V$、子区域数量 $R$ 等），通过公式计算复杂度分数 $s$，并映射到最佳迭代步数 $n^*$。
    *   **停滞检测：** 在推理过程中监控进度，若连续两次迭代未产生有效输出且已超过预测步数，则提前终止，以节省算力。

#### 3. 实验设计
*   **数据集：** 
    1.  **BraTS 2023 Meningioma：** 脑膜瘤 MRI 影像，包含四种模态（T1n, T1ce, T2w, T2-FLAIR）及分割掩码。
    2.  **MIMIC-CXR：** 胸部 X 射线摄影影像，包含 PA/Lateral/AP 等多视角。
*   **Benchmark 与对比：** 
    *   使用 **Gemini 2.5 Flash** 作为后端模型，且**无需任何任务特定的微调（Zero-shot）**。
    *   对比对象主要是传统的**单次推理 VLM 基准**。
*   **评估指标：** 诊断一致性（多次运行的重复性）、跨模态差异检测能力、报告的临床逻辑性。

#### 4. 资源与算力
*   **算力说明：** 论文未提及具体的本地 GPU 训练资源，因为该方法基于现有的基础模型 API（Gemini 2.5 Flash）。
*   **推理开销：** 详细记录了推理时间与 Token 消耗。例如，脑部 MRI 复杂病例约需 95.2 秒，消耗约 1.5 万个 Token；胸部 X 光病例约需 29 秒，消耗约 5500 个 Token。通过 RRouter，简单病例的计算成本降低了约 4 倍。

#### 5. 实验数量与充分性
*   **实验规模：** 针对典型病例进行了 16 次独立重复运行以测试可靠性。
*   **充分性评价：** 实验在定性分析和一致性验证上较为深入，展示了模型如何通过代码发现“FLAIR 信号与分割边界不符”等细微特征。然而，由于是研究原型，论文主要侧重于**案例研究（Case Studies）**，缺乏大规模数据集（如数千例）的自动化定量指标（如 F1 分数、AUC）对比，实验规模尚处于验证概念阶段。

#### 6. 主要结论与发现
*   **可审计性：** RVLM 成功将黑盒推理转化为可追溯的代码日志，满足了临床治理的透明度要求。
*   **跨模态洞察：** 相比单次推理，RVLM 能自主发现不同模态间的矛盾（如 T1ce 增强与 FLAIR 水肿范围的差异），这是单次推理模型难以捕捉的。
*   **自适应效率：** RRouter 能够准确预测任务复杂度，使简单任务快速结束，复杂任务深度推理，实现了精度与延迟的平衡。
*   **通用性：** 同一套框架仅需更换 Prompt（临床协议），即可从脑部 MRI 切换到胸部 X 光任务，无需重新训练。

#### 7. 优点（亮点）
*   **Trust-by-Design（设计即信任）：** 将监管合规性（如 EU AI Act）直接融入架构设计，而非事后补救。
*   **代码即推理：** 利用 Python REPL 增强了 VLM 的逻辑严密性，减少了幻觉。
*   **自适应计算：** 解决了迭代推理中“一刀切”的预算问题，提高了系统在实际部署中的经济性。

#### 8. 不足与局限
*   **评估规模有限：** 缺乏大规模的定量基准测试，难以全面评估其在所有临床场景下的敏感性和特异性。
*   **维度限制：** 目前主要处理 2D 轴向切片，尚未完全扩展到 3D 容积数据的全自动导航推理。
*   **特征依赖：** RRouter 目前依赖于特定领域的特征提取（如 BraTS 的分割统计），对于缺乏标注的数据集，路由器的泛化能力有待验证。
*   **临床验证缺失：** 尚未经过执业放射科医生的盲测或临床对照研究。

（完）
