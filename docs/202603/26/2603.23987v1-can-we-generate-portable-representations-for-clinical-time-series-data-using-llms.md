---
title: Can we generate portable representations for clinical time series data using LLMs?
title_zh: 我们能否利用大语言模型（LLMs）为临床时间序列数据生成可移植的表示？
authors: "Zongliang Ji, Yifei Sun, Andre Amaral, Anna Goldenberg, Rahul G. Krishnan"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.23987v1"
tags: ["query:medllm"]
score: 9.0
evidence: 利用大模型从ICU时间序列中提取患者嵌入表示
tldr: 针对临床机器学习模型在跨医院部署时因分布偏移导致性能下降的问题，本文提出利用大语言模型（LLM）生成可移植的患者表示。该方法将不规则的ICU时间序列数据转化为自然语言摘要，再通过文本嵌入模型生成固定长度向量。在多个数据集和任务上的实验表明，该方法在保持竞争力的同时，显著提升了跨医院迁移的鲁棒性和少样本学习能力，且未增加隐私风险，为临床预测模型的可扩展部署提供了新思路。
selection_source: fresh_fetch
motivation: 解决临床预测模型在不同医院间迁移时因分布偏移导致的性能下降和高昂的重新训练成本。
method: 利用冻结的LLM将ICU时间序列转化为自然语言摘要，再通过文本嵌入模型提取特征作为下游预测器的输入。
result: 该方法在多项任务中表现与主流基准相当，但在跨医院迁移时性能下降更小，且在少样本场景下表现更优。
conclusion: LLM生成的文本表示具有良好的可移植性和鲁棒性，能有效降低临床预测模型的工程开销并促进其规模化应用。
---

## 摘要
部署临床机器学习（ML）既缓慢又脆弱：在一家医院运行良好的模型在另一家医院往往会因分布偏移而性能下降。在这项工作中，我们研究了一个简单的问题——大语言模型（LLMs）能否创建可移植的患者嵌入，即能够使在一家医院构建的下游预测器在其他地方使用时只需极少甚至无需重新训练和微调的患者表示。为此，我们利用冻结的 LLM 将不规则的 ICU 时间序列映射为简洁的自然语言摘要，然后使用冻结的文本嵌入模型对每个摘要进行嵌入，从而获得一个定长向量，该向量可作为各种下游预测器的输入。在三个队列（MIMIC-IV、HIRID、PPICU）以及多个具有临床依据的预测和分类任务中，我们发现我们的方法简单易用，且在分布内表现上与网格插值、自监督表示学习以及时间序列基础模型相当，同时在迁移到新医院时表现出更小的相对性能下降。我们研究了不同提示词设计下的性能变化，发现结构化提示词对于在不改变平均准确率的情况下降低预测模型的方差至关重要。我们发现，使用这些可移植表示可以改善少样本学习，且相对于基准模型，并未增加年龄或性别等人口统计学信息的可恢复性，这表明其带来的额外隐私风险极小。我们的工作指出了 LLM 作为工具在通过减少工程开销来实现生产级预测模型的可扩展部署方面的潜力。

## Abstract
Deploying clinical ML is slow and brittle: models that work at one hospital often degrade under distribution shifts at the next. In this work, we study a simple question -- can large language models (LLMs) create portable patient embeddings i.e. representations of patients enable a downstream predictor built on one hospital to be used elsewhere with minimal-to-no retraining and fine-tuning. To do so, we map from irregular ICU time series onto concise natural language summaries using a frozen LLM, then embed each summary with a frozen text embedding model to obtain a fixed length vector capable of serving as input to a variety of downstream predictors. Across three cohorts (MIMIC-IV, HIRID, PPICU), on multiple clinically grounded forecasting and classification tasks, we find that our approach is simple, easy to use and competitive with in-distribution with grid imputation, self-supervised representation learning, and time series foundation models, while exhibiting smaller relative performance drops when transferring to new hospitals. We study the variation in performance across prompt design, with structured prompts being crucial to reducing the variance of the predictive models without altering mean accuracy. We find that using these portable representations improves few-shot learning and does not increase demographic recoverability of age or sex relative to baselines, suggesting little additional privacy risk. Our work points to the potential that LLMs hold as tools to enable the scalable deployment of production grade predictive models by reducing the engineering overhead.

---

## 论文详细总结（自动生成）

这篇论文探讨了利用大语言模型（LLM）为临床时间序列数据生成“可移植表示”的可能性，旨在解决临床预测模型在跨医院部署时面临的鲁棒性挑战。以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：临床机器学习模型在不同医疗机构间迁移时表现脆弱。由于不同医院的传感器模式、采样频率、临床策略和数据编码存在差异（分布偏移），在一个中心训练的模型直接应用到另一个中心时性能往往大幅下降。
*   **核心问题**：能否利用 LLM 将不规则、异构的 ICU 时间序列数据转化为一种“语义对齐且站点无关”的中间表示，从而使下游预测器无需大量重新训练即可在不同医院间通用？

### 2. 论文提出的方法论：Record2Vec
*   **核心思想**：采用“先摘要、后嵌入”（Summarize-then-Embed）的流水线，将数值型时间序列转化为自然语言，再转化为定长向量。
*   **技术流程**：
    1.  **序列序列化**：将 48 小时内的 ICU 不规则记录（实验室检查、生命体征、干预措施等）按时间戳和变量名序列化为文本。
    2.  **LLM 摘要生成**：使用**冻结（Frozen）**的 LLM（如 Gemini-2.0 Flash、MedGemma 或 Llama-3.1）根据特定提示词（Prompt）将原始记录总结为简洁的临床摘要。
    3.  **文本嵌入**：使用冻结的文本嵌入模型（如 Qwen3-Embedding）将摘要映射为固定长度的向量 $z_i$。
    4.  **下游预测**：将该向量输入轻量级的下游预测器（如 PatchTSMixer、MLP 或 LSTM）执行具体任务。
*   **关键细节**：整个过程中 LLM 和嵌入模型均保持冻结，不进行微调，以防止模型过度拟合特定站点的伪影。

### 3. 实验设计
*   **数据集**：使用了三个大型 ICU 队列：**MIMIC-IV**（美国）、**HiRID**（瑞士）和 **PPICU**（外部私有数据集）。
*   **预测任务**：
    *   **预测类**：未来 24 小时多变量预测、剩余住院时间（LoS）预测。
    *   **分类类**：院内死亡率预测、未来 24 小时药物使用（血管加压素/抗生素）预测、未来 24 小时实验室检查开立预测。
    *   **隐私探测**：性别分类和年龄回归（用于评估隐私泄露）。
*   **对比基准（Benchmarks）**：
    *   **网格插值流水线**：均值填充、前向填充（Right-shift）、线性插值。
    *   **自监督学习**：TSDE（时间序列扩散嵌入）。
    *   **时间序列基础模型**：TimesFM。
    *   **通用医疗框架**：GenHPF 的变体。

### 4. 资源与算力
*   **硬件**：实验主要在 **NVIDIA L40S GPU**（24GB 显存）上运行。
*   **算力分配**：
    *   推理和嵌入通常在单张 L40S 上完成。
    *   大规模参考运行使用了 **4 × L40S** 以提高吞吐量。
    *   LLM 推理使用了 **vLLM** 框架进行加速。
*   **成本/延迟**：Gemini-2.0 Flash 的推理延迟约为 0.26s/人，成本约为 0.7 美元/千人。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个数据集、7 项任务、4 种提示词策略、3 种不同的 LLM 摘要器以及多种下游架构。
*   **充分性**：
    *   进行了**分布内（In-distribution）**和**跨站点迁移（Cross-site transfer）**的全面对比。
    *   包含**少样本（Few-shot）**微调实验（仅用 16 个样本）。
    *   进行了**消融实验**，测试了不同嵌入模型、池化策略和提示词设计的影响。
*   **客观性**：通过 4 个随机种子取平均值，并与当前最先进的时间序列基础模型（TimesFM）进行对比，实验设计较为严谨。

### 6. 论文的主要结论与发现
*   **卓越的可移植性**：Record2Vec 在跨医院迁移任务中表现最强，在 10 组迁移实验中赢得了全部冠军，性能下降远小于基于数值的方法。
*   **分布内表现稳健**：在单医院内部测试中，Record2Vec 在 15 项任务中赢得了 13 项，证明语义摘要并未丢失关键预测信息。
*   **少样本效率高**：在仅有 16 个目标医院样本的情况下，Record2Vec 能够迅速恢复性能，接近全量数据训练的效果。
*   **隐私安全性**：与原始数据相比，Record2Vec 生成的表示并未增加年龄和性别等敏感信息的泄露风险。
*   **提示词敏感度**：结构化提示词（如 ICD 风格）能有效降低预测方差，但对平均准确率影响较小。

### 7. 优点
*   **语义对齐**：通过自然语言中和了不同医院在单位、变量命名和采样频率上的差异。
*   **工程开销低**：无需为每个新站点重新设计复杂的特征工程或数据清洗流水线。
*   **效率提升**：摘要后的文本长度仅为原始序列的 1/25，显著降低了嵌入模型的计算负担。

### 8. 不足与局限
*   **隐私合规风险**：目前依赖外部 LLM API（如 Gemini），在实际临床应用中需解决数据出域的合规问题（需转向本地化部署）。
*   **推理延迟**：尽管比直接处理长序列快，但 LLM 推理仍比传统数值模型慢，可能限制实时预警应用。
*   **理论深度有限**：研究主要基于经验性实验，缺乏关于“为什么自然语言摘要能实现站点不变性”的深层理论证明（如因果图分析）。
*   **信息丢失风险**：摘要过程是一个有损压缩过程，虽然对宏观预测有效，但可能丢失某些极高频的微小波动信号。

（完）
