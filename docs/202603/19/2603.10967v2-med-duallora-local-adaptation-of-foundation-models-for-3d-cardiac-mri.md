---
title: "Med-DualLoRA: Local Adaptation of Foundation Models for 3D Cardiac MRI"
title_zh: Med-DualLoRA：面向 3D 心脏磁共振成像的基础模型本地自适应
authors: "Joan Perramon-Llussà, Amelia Jiménez-Sánchez, Grzegorz Skorupko, Fotis Avgoustidis, Carlos Martín-Isla, Karim Lekadir, Polyxeni Gkontra"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10967v2"
tags: ["query:medllm"]
score: 8.0
evidence: 针对医学影像基础模型的联邦微调
tldr: 针对3D心脏MRI基础模型在多中心联邦学习中面临的非独立同分布数据和高通信开销问题，本文提出Med-DualLoRA框架。该方法通过加性分解将LoRA分为全局共享和本地私有模块，仅同步全局参数以保护隐私并降低成本。实验表明，该方法在疾病检测任务中显著优于现有联邦微调基准，为医疗基础模型的本地化适配提供了高效、可扩展的方案。
selection_source: carryover_cache
motivation: 旨在解决医疗基础模型在多中心联邦微调中因数据异构性导致的性能下降、高昂通信成本及隐私保护需求。
method: 提出一种客户端感知的参数高效微调框架，通过加性分解将LoRA拆分为全局共享组件和本地私有组件，且仅在站点间聚合全局部分。
result: 在多中心3D心脏MRI疾病检测任务中，该方法在显著降低通信开销的同时，取得了优于现有联邦微调基准的平衡准确率和特异性。
conclusion: Med-DualLoRA为临床约束下的医疗基础模型本地化适配提供了一种兼顾隐私、效率与个性化性能的可扩展解决方案。
---

## 摘要
基础模型（FMs）在经过特定任务的自适应后，在包括心脏磁共振（CMR）在内的各种医学成像任务和模态中展现出强大的下游性能潜力。然而，使用单中心数据进行自适应可能会导致性能欠佳并增加模型偏差，而由于隐私限制，对临床数据进行集中式微调通常是不可行的。联邦微调提供了一种保护隐私的替代方案；然而，传统方法在异构、非独立同分布（non-IID）的多中心数据下表现不佳，且在自适应大型模型时会产生巨大的通信开销。在这项工作中，我们研究了用于 3D CMR 疾病检测的联邦基础模型微调，并提出了 Med-DualLoRA，这是一种客户端感知的参数高效微调（PEFT）联邦框架，通过加法分解将全局共享和本地低秩自适应（LoRA）解耦。全局和本地 LoRA 模块在本地进行训练，但只有全局组件在各站点之间共享和聚合，从而保持本地适配器的私密性。这种设计在显著降低通信成本的同时提高了个性化水平，实验表明，仅对两个 Transformer 块进行自适应即可在保持性能的同时进一步提高效率。我们在 ACDC 和 M&Ms 组合数据集上，针对用于疾病检测的先进多中心电影 3D CMR 基础模型评估了我们的方法，并将每个厂商视为一个联邦客户端。与其它联邦 PEFT 基准相比，Med-DualLoRA 实现了具有统计学意义的性能提升（平衡准确率 0.768，特异性 0.612），同时保持了通信效率。我们的方法为现实临床约束下医学基础模型的本地联邦自适应提供了一种可扩展的解决方案。

## Abstract
Foundation models (FMs) show great promise for robust downstream performance across medical imaging tasks and modalities, including cardiac magnetic resonance (CMR), following task-specific adaptation. However, adaptation using single-site data may lead to suboptimal performance and increased model bias, while centralized fine-tuning on clinical data is often infeasible due to privacy constraints. Federated fine-tuning offers a privacy-preserving alternative; yet conventional approaches struggle under heterogeneous, non-IID multi-center data and incur substantial communication overhead when adapting large models. In this work, we study federated FM fine-tuning for 3D CMR disease detection and propose Med-DualLoRA, a client-aware parameter-efficient fine-tuning (PEFT) federated framework that disentangles globally shared and local low-rank adaptations (LoRA) through additive decomposition. Global and local LoRA modules are trained locally, but only the global component is shared and aggregated across sites, keeping local adapters private. This design improves personalization while significantly reducing communication cost, and experiments show that adapting only two transformer blocks preserves performance while further improving efficiency. We evaluate our method on a multi-center state-of-the-art cine 3D CMR FM fine-tuned for disease detection using ACDC and combined M\&Ms datasets, treating each vendor as a federated client. Med-DualLoRA achieves statistically significant improved performance (balanced accuracy 0.768, specificity 0.612) compared to other federated PEFT baselines, while maintaining communication efficiency. Our approach provides a scalable solution for local federated adaptation of medical FMs under realistic clinical constraints.

---

## 论文详细总结（自动生成）

以下是对论文《Med-DualLoRA: Local Adaptation of Foundation Models for 3D Cardiac MRI》的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：心脏磁共振成像（CMR）是评估心脏结构和功能的金标准，但深度学习模型在跨中心应用时，常因设备厂商、扫描协议和标注差异面临泛化性差的问题。
*   **核心问题**：
    1.  **隐私限制**：医疗数据敏感，难以进行集中式微调。
    2.  **异构性挑战**：联邦学习（FL）在处理非独立同分布（non-IID）的多中心数据时，全局模型往往难以兼顾各站点的个性化特征。
    3.  **通信开销**：基础模型（FM）参数量巨大，在联邦学习中频繁传输更新会产生极高的通信成本。
*   **研究动机**：提出一种既能保护隐私、降低通信成本，又能通过“全局-本地”解耦来应对数据异构性的 3D 医学影像基础模型微调框架。

### 2. 方法论：Med-DualLoRA
*   **核心思想**：在 LoRA（低秩自适应）的基础上，通过**加法分解**将适配器拆分为“全局共享”和“本地私有”两个独立模块。
*   **关键技术细节**：
    *   **双路径适配**：在 Transformer 的线性投影层中注入两个并行的低秩矩阵对。公式表达为：$f_i(x) = Wx + \Delta W_{global}x + \Delta W_{local,i}x$。
    *   **参数分离**：
        *   **全局 LoRA ($A_g, B_g$)**：学习跨站点的通用特征，参与联邦聚合。
        *   **本地 LoRA ($A_{li}, B_{li}$)**：捕捉站点特有的分布特征，始终保留在本地，不参与共享。
    *   **算法流程**：客户端在本地同时训练全局和本地适配器，但仅将全局适配器的更新发送至服务器；服务器利用 FedAvg 算法聚合全局参数并分发回客户端。
    *   **效率优化**：研究了仅对部分 Transformer 块（如仅最后两块）进行适配对性能与通信成本的影响。

### 3. 实验设计
*   **数据集**：
    *   **ACDC**：150 名患者的受控采集数据。
    *   **M&Ms (1 & 2)**：543 名患者，涵盖 Siemens、GE、Philips、Canon 四大厂商，反映了真实的临床异构性。
*   **任务场景**：基于 3D CMR 的二分类疾病检测。
*   **对比基准 (Benchmarks)**：
    *   **集中式 (Upper Bound)**：全量微调 (Full-ft)、仅头部微调 (Head-only)、标准 LoRA。
    *   **联邦式 (Baselines)**：联邦 Head-only、联邦标准 LoRA。
    *   **本地化**：仅使用本地数据训练的独立模型。

### 4. 资源与算力
*   **硬件设备**：
    *   集中式实验：单块 **NVIDIA GeForce RTX 4090** GPU。
    *   联邦式实验：双 **NVIDIA L40S** 系统（用于模拟多客户端并发更新）。
*   **训练细节**：使用 AdamW 优化器；集中式训练最多 500 epoch（带早停）；联邦学习每轮进行 1 次本地 epoch。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个主要的联邦客户端（4 个厂商 + ACDC 站点），进行了跨中心泛化实验。
*   **消融实验**：针对 LoRA 插入的 Transformer 块数量（1 到 12 块）进行了详细的帕累托前沿（Pareto frontier）分析，探讨通信预算与准确率的平衡。
*   **客观性**：所有实验均运行 3 个随机种子并取平均值，使用配对 t 检验进行统计显著性分析。实验设计考虑了类别不平衡问题，采用了平衡准确率（Balanced Accuracy）等指标，评价较为客观公平。

### 6. 主要结论与发现
*   **性能提升**：Med-DualLoRA 在联邦设置下达到了 **0.768** 的平衡准确率，显著优于标准联邦 LoRA（0.693）和 Head-only 方法。
*   **解决塌陷问题**：传统联邦 PEFT 在异构数据下特异性（Specificity）极低（接近 0），而 Med-DualLoRA 将其提升至 **0.612**，有效缓解了模型对多数类的偏差。
*   **高效通信**：实验发现，仅适配 **2 个 Transformer 块**即可获得接近全量适配的性能，每轮通信开销仅约 **28 MB**，在帕累托前沿上表现最优。
*   **缩小差距**：该方法成功缩小了联邦学习与集中式训练之间的性能差距，证明了结构化解耦的有效性。

### 7. 优点与亮点
*   **结构创新**：首次在 3D 医学影像基础模型中引入显式的全局与本地低秩适配器分离机制。
*   **隐私与性能兼顾**：本地适配器不共享，既增强了隐私保护，又通过个性化参数提升了在异构数据上的鲁棒性。
*   **实用性强**：针对临床实际中的通信带宽限制，提供了明确的参数量与性能权衡建议。

### 8. 不足与局限
*   **数据划分偏差**：沿用了原始数据集的划分，这可能导致某些客户端内部的类别极度不平衡（如某些厂商数据中患病比例极高），未来可尝试分层抽样。
*   **聚合策略单一**：目前仅使用了 FedAvg，尚未探索如 FedProx 或 Scaffold 等更先进的联邦优化算法与该框架的结合。
*   **任务覆盖面**：目前仅验证了疾病检测（分类）任务，尚未在分割或回归等其他 CMR 任务上进行验证。

（完）
