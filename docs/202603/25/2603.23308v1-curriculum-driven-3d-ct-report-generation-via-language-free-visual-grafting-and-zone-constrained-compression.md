---
title: Curriculum-Driven 3D CT Report Generation via Language-Free Visual Grafting and Zone-Constrained Compression
title_zh: 基于无语言视觉嫁接与区域约束压缩的课程驱动型 3D CT 报告生成
authors: "V. K. Cody Bumgardner, Mitchell A. Klusty, Mahmut S. Gokmen, Evan W. Damron"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23308v1"
tags: ["query:medllm"]
score: 9.0
evidence: 使用适配的Llama 3.2模型进行课程驱动的放射报告生成
tldr: 该研究针对3D CT影像报告生成中序列过长、类别不平衡及大语言模型过度依赖文本先验的问题，提出了Ker-VLJEPA-3B框架。该框架采用四阶段课程学习，通过无语言监督的自监督编码器提取纯视觉特征，并利用区域约束交叉注意力将切片嵌入压缩为32个空间定位视觉Token。实验表明，该方法在CT-RATE基准上显著超越了现有SOTA模型，有效提升了报告生成的视觉相关性。
selection_source: fresh_fetch
motivation: 解决3D CT报告生成中视觉Token被LLM忽略、数据不平衡以及长序列处理困难等挑战。
method: 提出一种基于四阶段课程学习的框架，结合无语言视觉骨干网络、区域约束压缩技术及PCA白化等创新手段实现视觉与语言的深度对齐。
result: "在CT-RATE基准测试中，该模型达到0.429的宏F1分数，优于当前最先进的U-VLM模型，且视觉内容对生成质量的贡献占比超过56%。"
conclusion: 通过课程学习和空间约束压缩，可以有效将自监督视觉特征整合进LLM，显著提升3D医疗影像报告生成的准确性和视觉忠实度。
---

## 摘要
从 3D 计算机断层扫描（CT）体数据中自动生成放射学报告面临巨大挑战，原因在于极长的序列长度、严重的类别不平衡，以及大语言模型（LLM）倾向于忽略视觉标记而偏向语言先验。我们提出了 Ker-VLJEPA-3B，这是一个用于从胸部 CT 体数据生成自由文本报告的四阶段课程学习框架。该分阶段训练课程逐步使 Llama 3.2 3B 解码器适应，将其输出建立在来自冻结的自监督编码器的视觉特征之上。我们的视觉主干网络（LeJEPA ViT-Large）通过在无标签 CT 上进行自监督联合嵌入预测进行训练，无需文本监督。与对比模型（如 CLIP、BiomedCLIP）不同，这种无语言主干网络产生的是模态纯净的表示。视觉-语言对齐被推迟到课程的桥接和生成阶段。这种模态无关的设计可以在基础训练期间，在没有配对文本的情况下，将任何自监督编码器集成到 LLM 中。方法论创新包括：（1）区域约束的交叉注意力机制，将切片嵌入压缩为 32 个具有空间定位的视觉标记；（2）对各向异性 LLM 嵌入进行 PCA 白化；（3）消除后验崩溃的“仅阳性发现”策略；（4）转移投影权重的热桥接初始化；以及（5）结合弹性权重巩固（EWC）的选择性交叉注意力冻结，以防止灾难性遗忘。在 CT-RATE 基准测试（2,984 个验证体数据，18 个类别）上的评估结果显示，Ker-VLJEPA-3B 的宏 F1 达到 0.429，比现有最先进模型（U-VLM，宏 F1 = 0.414）高出 3.6%，并在阈值优化后达到 0.448（+8.2%）。消融研究证实，56.6% 的生成质量源自患者特定的视觉内容。代码和权重已公开。

## Abstract
Automated radiology report generation from 3D computed tomography (CT) volumes is challenging due to extreme sequence lengths, severe class imbalance, and the tendency of large language models (LLMs) to ignore visual tokens in favor of linguistic priors. We present Ker-VLJEPA-3B, a four-phase curriculum learning framework for free-text report generation from thoracic CT volumes. A phased training curriculum progressively adapts a Llama 3.2 3B decoder to ground its output in visual features from a frozen, self-supervised encoder. Our visual backbone (LeJEPA ViT-Large) is trained via self-supervised joint-embedding prediction on unlabeled CTs, without text supervision. Unlike contrastive models (CLIP, BiomedCLIP), this language-free backbone yields modality-pure representations. Vision-language alignment is deferred to the curriculum's bridge and generation phases. This modality-agnostic design can integrate any self-supervised encoder into an LLM without paired text during foundation training. Methodological innovations include: (1) zone-constrained cross-attention compressing slice embeddings into 32 spatially-grounded visual tokens; (2) PCA whitening of anisotropic LLM embeddings; (3) a positive-findings-only strategy eliminating posterior collapse; (4) warm bridge initialization transferring projection weights; and (5) selective cross-attention freezing with elastic weight consolidation to prevent catastrophic forgetting. Evaluated on the CT-RATE benchmark (2,984 validation volumes, 18 classes), Ker-VLJEPA-3B achieves a macro F1 of 0.429, surpassing the state-of-the-art (U-VLM, macro F1 = 0.414) by 3.6%, and reaching 0.448 (+8.2%) with threshold optimization. Ablation studies confirm 56.6% of generation quality derives from patient-specific visual content. Code and weights are available.

---

## 论文详细总结（自动生成）

### 论文结构化深度总结：Ker-VLJEPA-3B

#### 1. 核心问题与研究动机
*   **核心问题**：从 3D CT 体数据自动生成放射学报告面临三大挑战：
    1.  **序列极长**：单次扫描包含数百张切片，远超标准大语言模型（LLM）的上下文窗口。
    2.  **类别极度不平衡**：正常解剖描述占据报告的 90%，导致模型倾向于生成通用的“正常”描述。
    3.  **后验崩溃（Posterior Collapse）**：LLM 强大的语言先验使其在生成时倾向于忽略注入的视觉 Token，转而依赖文本上下文。
*   **研究动机**：现有方法多依赖于受文本监督的视觉编码器（如 CLIP），这会引入语言偏见。本文旨在通过“无语言”视觉特征嫁接和分阶段课程学习，实现更纯粹、更具视觉忠实度的 3D 医疗报告生成。

#### 2. 核心方法论
*   **核心思想**：采用**解耦设计**，将视觉表示学习与语言对齐完全分离。使用纯自监督学习的视觉主干，通过四阶段课程学习逐步引导 LLM 理解视觉特征。
*   **关键技术细节**：
    *   **区域约束交叉注意力（Zone-Constrained Cross-Attention）**：将 z 轴划分为 32 个连续解剖区域，每个区域由一个可学习查询负责，将数百张切片压缩为 32 个具有空间定位意义的视觉 Token。
    *   **PCA 白化（PCA Whitening）**：针对 LLM 文本嵌入严重的各向异性（各向量挤在狭窄锥体内），通过 PCA 将其投影到 256 维等向空间，使对比学习（InfoNCE）的判别力提升 11.8 倍。
    *   **仅阳性发现训练（Positive-Findings-Only Training）**：在生成训练阶段剔除正常描述，强制模型梯度关注病理特征，解决正常文本梯度占主导导致的后验崩溃。
    *   **四阶段课程学习流程**：
        1.  **视觉对齐**：训练编码器进行多标签分类。
        2.  **对比桥接**：在白化空间进行视觉-文本对比对齐。
        3.  **生成微调**：使用“仅阳性发现”文本训练 LLM 的视觉感知生成。
        4.  **叙述适配**：使用原始报告微调，配合**弹性权重巩固（EWC）**和选择性冻结，防止病理检测能力的灾难性遗忘。
    *   **热桥接初始化（Warm Bridge Initialization）**：在阶段间转移已收敛的投影层和适配器权重，避免从头训练导致的性能回退。

#### 3. 实验设计
*   **数据集**：使用 **CT-RATE** 基准数据集，包含约 5 万个训练体数据和 2,984 个验证体数据（涵盖 18 类胸部异常）。
*   **Benchmark**：采用官方 RadBERT 标签提取协议，将生成的自由文本报告转化为 18 类二元标签进行评估。
*   **对比方法**：对比了 CT-CLIP（对比学习）、CT-CHAT（对话式 VLM）、BTB3D（小波压缩）以及之前的 SOTA 模型 **U-VLM**（分层视觉注入）。

#### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA H200 GPU**。
*   **训练细节**：采用分布式数据并行（DDP）和 bf16 混合精度。各阶段训练轮数不等（阶段 1: 20 轮；阶段 2: 24 轮；阶段 3: 9 轮；阶段 4: 8 轮）。

#### 5. 实验数量与充分性
*   **实验规模**：在近 3,000 个验证样本上进行了全面测试。
*   **消融实验**：设计了极其详尽的四部分消融研究：
    1.  **线性探测**：验证各阶段视觉信息的保留程度。
    2.  **生成消融**：通过置零、随机化、打乱视觉 Token 证明 56.6% 的生成质量直接源自视觉内容。
    3.  **负对数似然（NLL）分析**：证明视觉 Token 在病理相关词汇上的贡献是通用词汇的 2 倍。
    4.  **组件消融**：量化了“仅阳性训练”、“热桥接”等创新的具体增益。
*   **评价**：实验设计非常客观、严谨，通过多种手段排除了模型仅靠语言先验“盲猜”的可能性。

#### 6. 主要结论与发现
*   **性能领先**：Ker-VLJEPA-3B 在宏 F1 上达到 **0.429**，超越 U-VLM（0.414），建立新 SOTA。
*   **视觉忠实度**：证明了即使视觉编码器从未见过任何文本，也能通过课程学习有效地嫁接到 LLM 中。
*   **召回率优势**：相比 U-VLM，该模型在召回率上提升了 22.1%，更适合作为临床“安全网”防止漏诊。

#### 7. 优点与亮点
*   **模态无关性**：框架设计不依赖特定模态的文本对，理论上可接入基因组、传感器等任何自监督编码器。
*   **空间定位**：区域约束机制赋予了视觉 Token 明确的解剖学意义（如 Token 0 对应肺尖，Token 31 对应肺底）。
*   **解决后验崩溃**：通过创新的训练策略（仅阳性文本 + EWC），成功让 LLM 在长文本生成中保持对视觉细节的关注。

#### 8. 不足与局限
*   **精确率（Precision）权衡**：虽然召回率极高，但精确率低于 U-VLM，可能存在一定的过度报告风险。
*   **单一基准测试**：目前仅在 CT-RATE 数据集上验证，跨中心、跨设备的泛化性有待进一步考察。
*   **算力门槛**：训练过程依赖 8 张 H200，对于普通研究机构而言成本较高。
*   **缺乏临床医生评估**：虽然自动指标领先，但尚未进行正式的放射科医生人类观察者研究。

（完）
