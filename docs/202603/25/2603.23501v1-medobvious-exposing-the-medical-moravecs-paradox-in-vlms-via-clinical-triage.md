---
title: "MedObvious: Exposing the Medical Moravec's Paradox in VLMs via Clinical Triage"
title_zh: MedObvious：通过临床分诊揭示视觉语言模型中的医学莫拉维克悖论
authors: "Ufaq Khan, Umair Nawaz, L D M S S Teja, Numaan Saeed, Muhammad Bilal, Yutong Xie, Mohammad Yaqub, Muhammad Haris Khan"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23501v1"
tags: ["query:medllm"]
score: 9.0
evidence: 针对医疗视觉语言模型在临床分诊和输入验证方面的基准测试
tldr: "本文针对视觉语言模型（VLM）在医疗诊断中忽视基础输入验证的问题，提出了MedObvious基准测试。该基准包含1,880个任务，涵盖从基础方向到临床分诊的五个层级，旨在评估模型识别图像一致性和有效性的能力。研究发现，即使是先进的VLM在处理医疗图像的“常识性”检查（如模态、解剖结构验证）时仍表现不佳，容易产生幻觉且鲁棒性不足，揭示了医疗AI领域存在的莫拉维克悖论。"
selection_source: fresh_fetch
motivation: 现有医疗VLM评估忽略了诊断前的基础输入验证，导致模型可能在无效或不一致的图像输入上生成误导性的诊断报告。
method: "引入MedObvious基准，通过五个层级的1,880个多图一致性任务，测试模型对医疗图像模态、解剖结构及临床分诊线索的校验能力。"
result: 对17个VLM的评估表明，模型在基础验证上极不可靠，容易在正常输入中幻觉出异常，且性能随图像规模增加而显著下降。
conclusion: 预诊断验证是医疗VLM尚未解决的安全关键能力，在临床部署前必须将其作为独立的评估维度进行严格测试。
---

## 摘要
视觉语言模型（VLM）正越来越多地应用于医疗报告生成和视觉问答等任务。然而，流畅的诊断文本并不保证安全的视觉理解。在临床实践中，影像解读始于诊断前的完整性检查：验证输入是否有效且可读（模态和解剖结构正确、视角和方向合理，且无明显的完整性违规）。现有的基准测试在很大程度上假设这一步骤已得到解决，因此忽略了一个关键的失效模式：即使输入不一致或无效，模型仍能生成看似合理的叙述。我们推出了 MedObvious，这是一个包含 1,880 个任务的基准测试，它将输入验证隔离为针对小型多面板图像集的集合级一致性能力：模型必须识别是否有任何面板违反了预期的连贯性。MedObvious 涵盖五个递进层级，从基础的方向/模态不匹配到基于临床动机的解剖结构/视角验证以及分诊式线索，并包含五种评估格式以测试跨界面的鲁棒性。通过对 17 种不同的 VLM 进行评估，我们发现完整性检查仍然不可靠：多个模型在正常（阴性对照）输入上幻觉出异常，性能随图像集规模扩大而下降，且在多选题和开放式设置之间测得的准确率存在显著差异。这些结果表明，对于医疗 VLM 而言，诊断前验证仍未解决，在部署前应将其视为一种独立的、关乎安全的关键能力。

## Abstract
Vision Language Models (VLMs) are increasingly used for tasks like medical report generation and visual question answering. However, fluent diagnostic text does not guarantee safe visual understanding. In clinical practice, interpretation begins with pre-diagnostic sanity checks: verifying that the input is valid to read (correct modality and anatomy, plausible viewpoint and orientation, and no obvious integrity violations). Existing benchmarks largely assume this step is solved, and therefore miss a critical failure mode: a model can produce plausible narratives even when the input is inconsistent or invalid. We introduce MedObvious, a 1,880-task benchmark that isolates input validation as a set-level consistency capability over small multi-panel image sets: the model must identify whether any panel violates expected coherence. MedObvious spans five progressive tiers, from basic orientation/modality mismatches to clinically motivated anatomy/viewpoint verification and triage-style cues, and includes five evaluation formats to test robustness across interfaces. Evaluating 17 different VLMs, we find that sanity checking remains unreliable: several models hallucinate anomalies on normal (negative-control) inputs, performance degrades when scaling to larger image sets, and measured accuracy varies substantially between multiple-choice and open-ended settings. These results show that pre-diagnostic verification remains unsolved for medical VLMs and should be treated as a distinct, safety-critical capability before deployment.

---

## 论文详细总结（自动生成）

### MedObvious：通过临床分诊揭示视觉语言模型中的医学莫拉维克悖论

#### 1. 论文的核心问题与整体含义
论文提出了**“医学莫拉维克悖论” (Medical Moravec’s Paradox)**：视觉语言模型（VLM）虽然能生成流利的医学诊断报告，但在处理人类看来极其简单的“预诊断完整性检查”（如识别图像方向是否错误、模态是否匹配、解剖部位是否正确）时却表现得异常脆弱。
*   **研究动机**：在临床实践中，医生在诊断前会先进行“分诊式”校验。如果输入图像无效（如部位错、模态错、伪影严重），后续的诊断报告无论多流利都是危险的。
*   **核心目标**：填补现有基准测试（如 VQA-RAD 等）只关注诊断结果而忽略输入验证的空白，评估 VLM 作为临床“守门员”的安全性。

#### 2. 论文提出的方法论
论文引入了 **MedObvious** 基准测试，其核心思想是将输入验证抽象为**集合级的一致性检测任务**：
*   **任务构建**：将医学图像排列成 $2 \times 2$ 或 $3 \times 3$ 的网格。模型需要从一组图像中识别出“离群值”（Outlier，即在模态、解剖结构或方向上不一致的图像），或者确认所有图像均一致（阴性对照）。
*   **五个递进层级 (T1-T5)**：
    1.  **T1 基础层**：检测基本的方向（翻转）和模态（如 CT vs MRI）不匹配。
    2.  **T2 多样性层**：测试跨不同数据源的鲁棒性。
    3.  **T3 规模扩展层**：使用 $3 \times 3$ 网格，增加干扰项，测试模型的系统性比较能力。
    4.  **T4 语义层**：验证解剖结构和视角（如胸部 vs 腹部，正位 vs 侧位）。
    5.  **T5 分诊层**：识别高显著性的临床失效线索和跨模态连贯性中断。
*   **五种评估格式**：包括多选题（MCQ）、开放式回答、视觉指代（Yes/No）等，旨在排除模型对特定交互界面的偏好。

#### 3. 实验设计
*   **数据集来源**：主要基于 ROCO（放射学图像）和 Kvasir（内窥镜图像）进行过滤和模板化生成。
*   **Benchmark 规模**：共 1,880 个任务，其中 37.5% 为阴性对照（无离群值），用于检测模型的“虚假报警”率。
*   **对比方法**：评估了 17 个模型，分为三类：
    *   **通用开源 VLM**：如 LLaVA-1.5, Qwen2/2.5/3-VL, InternVL2.5/3, Pixtral。
    *   **医疗专用开源 VLM**：如 LLaVA-Med, MedGemma, Lingshu, Fleming。
    *   **闭源/商业 VLM**：如 GPT-4o, GPT-5-nano, Gemini 2.0/2.5 Flash。
*   **评估设置**：采用 **Zero-shot（零样本）** 推理，不进行微调或少样本提示。

#### 4. 资源与算力
*   **硬件使用**：开源模型的评估在 **NVIDIA A100 (40 GB) GPU** 上运行。
*   **闭源模型**：通过公共 API 进行查询。
*   **训练时长**：由于该论文侧重于基准测试评估而非模型训练，文中未提及训练时长，主要算力消耗在于 17 个模型在 1,880 个任务上的推理过程。

#### 5. 实验数量与充分性
*   **实验规模**：论文对 17 个模型在 5 种任务格式、5 个难度层级上进行了全面测试，总计产生了大量的推理样本。
*   **充分性**：实验设计非常充分。通过引入“阴性对照”，有效地揭示了模型在正常图像上产生幻觉的倾向；通过对比 MCQ 和开放式回答，揭示了模型在不同提示词下的不稳定性。
*   **公平性**：所有模型均使用统一的推理框架和标准化的输出解析逻辑，确保了对比的客观性。

#### 6. 主要结论与发现
*   **验证能力不可靠**：即使是性能最好的模型，平均准确率也仅为 63.2%，远低于人类专家（88.4%）。
*   **严重的“虚假报警”**：许多模型存在“总想找点茬”的偏见，在完全正常的图像集中频繁幻觉出不存在的异常。
*   **规模敏感性**：当网格从 $2 \times 2$ 扩展到 $3 \times 3$ 时，模型性能显著下降，说明其缺乏系统性的全局比较能力。
*   **格式依赖性**：模型在多选题上表现较好，但在开放式描述中表现较差，说明其视觉理解并未真正与语言生成对齐。

#### 7. 优点
*   **视角独特**：首次正式定义并量化了医疗 VLM 中的“莫拉维克悖论”，关注安全关键的预诊断阶段。
*   **设计严谨**：通过五个层级和阴性对照组，能够精准定位模型在视觉感知上的具体短板。
*   **临床相关性强**：任务设计模拟了放射科医生使用 3D Slicer 等软件查看多面板影像的真实场景。

#### 8. 不足与局限
*   **场景简化**：使用了静态的 2D 网格，而真实的临床工作流通常涉及 3D 序列（如 CT/MRI 切片流）和交互式操作。
*   **数据多样性限制**：虽然涵盖了多种模态，但主要依赖于过滤后的公开数据集，可能无法完全覆盖罕见的临床伪影或复杂的设备干扰。
*   **应用限制**：目前仅作为离线评估工具，尚未在实时医疗 AI 代理（Agentic AI）中进行闭环验证。

（完）
