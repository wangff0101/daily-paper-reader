---
title: "MedSPOT: A Workflow-Aware Sequential Grounding Benchmark for Clinical GUI"
title_zh: MedSPOT：针对临床 GUI 的工作流感知顺序定位基准
authors: "Rozain Shakeel, Abdul Rahman Mohammad Ali, Muneeb Mushtaq, Tausifa Jan Saleem, Tajamul Ashraf"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19993v1"
tags: ["query:medllm"]
score: 8.0
evidence: 临床软件界面中多模态大模型的基准测试
tldr: 针对多模态大模型在临床GUI环境中缺乏序列化工作流推理能力的问题，本文提出了MedSPOT基准。该基准包含216个任务驱动视频和597个关键帧标注，模拟了真实医疗工作流中的多步连续交互。通过引入严格的序列评估协议和详细的故障分类体系，MedSPOT为评估模型在复杂医疗软件中的空间定位和程序化推理能力提供了高标准、安全关键的测试平台。
selection_source: fresh_fetch
motivation: 现有的GUI基准多关注单步定位，忽视了真实医疗场景中跨步骤、动态变化的序列化工作流推理需求。
method: 开发了包含216个多步任务视频的MedSPOT基准，并设计了首错即止的严格序列评估协议及细粒度的故障分类体系。
result: 该基准通过模拟真实医疗工作流，揭示了现有模型在处理界面层级、上下文依赖及微小目标定位时的错误传播问题。
conclusion: MedSPOT通过将评估从孤立定位转向工作流感知的序列推理，为医疗软件环境下的多模态模型评估建立了更真实且关注安全性的标准。
---

## 摘要
尽管多模态大语言模型（MLLMs）取得了快速进展，但它们在高风险临床软件环境中执行可靠视觉定位的能力仍未得到充分探索。现有的 GUI 基准主要关注孤立的单步定位查询，忽略了现实医疗界面中所需的顺序、工作流驱动的推理，在这些界面中，任务跨越独立步骤和动态界面状态演变。我们推出了 MedSPOT，这是一个针对临床 GUI 环境的工作流感知顺序定位基准。与以往将定位视为独立预测任务的基准不同，MedSPOT 将程序化交互建模为一系列结构化的空间决策。该基准包含 216 个任务驱动的视频和 597 个标注的关键帧，其中每个任务由现实医疗工作流中的 2 到 3 个相互依赖的定位步骤组成。这种设计捕捉了演变条件下的界面层级、上下文依赖关系以及细粒度的空间精度。为了评估程序鲁棒性，我们提出了一种严格的顺序评估协议，该协议在出现第一个错误的定位预测时即终止任务评估，从而明确衡量多步工作流中的错误传播。我们还引入了一个全面的失败分类法，包括边缘偏差、小目标错误、无预测、近距失误、远距失误和工具栏混淆，以便对临床 GUI 环境中的模型行为进行系统诊断。通过将评估从孤立的定位转向工作流感知的顺序推理，MedSPOT 为评估医疗软件环境中的多模态模型建立了一个现实且安全关键的基准。代码和数据可在以下网址获取：https://github.com/Tajamul21/MedSPOT。

## Abstract
Despite the rapid progress of Multimodal Large Language Models (MLLMs), their ability to perform reliable visual grounding in high-stakes clinical software environments remains underexplored. Existing GUI benchmarks largely focus on isolated, single-step grounding queries, overlooking the sequential, workflow-driven reasoning required in real-world medical interfaces, where tasks evolve across independent steps and dynamic interface states. We introduce MedSPOT, a workflow-aware sequential grounding benchmark for clinical GUI environments. Unlike prior benchmarks that treat grounding as a standalone prediction task, MedSPOT models procedural interaction as a sequence of structured spatial decisions. The benchmark comprises 216 task-driven videos with 597 annotated keyframes, in which each task consists of 2 to 3 interdependent grounding steps within realistic medical workflows. This design captures interface hierarchies, contextual dependencies, and fine-grained spatial precision under evolving conditions. To evaluate procedural robustness, we propose a strict sequential evaluation protocol that terminates task assessment upon the first incorrect grounding prediction, explicitly measuring error propagation in multi-step workflows. We further introduce a comprehensive failure taxonomy, including edge bias, small-target errors, no prediction, near miss, far miss, and toolbar confusion, to enable systematic diagnosis of model behavior in clinical GUI settings. By shifting evaluation from isolated grounding to workflow-aware sequential reasoning, MedSPOT establishes a realistic and safety-critical benchmark for assessing multimodal models in medical software environments. Code and data are available at: https://github.com/Tajamul21/MedSPOT.

---

## 论文详细总结（自动生成）

### 论文总结：MedSPOT——针对临床 GUI 的工作流感知顺序定位基准

#### 1. 核心问题与整体含义（研究动机和背景）
现代医疗高度依赖复杂的图形用户界面（GUI），如放射影像查看器和电子病历系统。然而，现有的多模态大模型（MLLM）在 GUI 定位（Grounding）上的研究主要集中在通用网页或桌面自动化，且多为孤立的单步任务。
*   **研究动机**：医疗 GUI 具有布局密集、层级复杂、专业术语多且对精度要求极高的特点。
*   **核心问题**：现有的基准测试忽视了真实医疗场景中“工作流”的连续性。在多步操作中，前一步的错误会直接导致后续步骤失效。目前缺乏一个能够评估模型在临床软件中进行**序列化、工作流感知**定位能力的基准。

#### 2. 方法论：核心思想与技术细节
MedSPOT 将 GUI 定位建模为一个**受指令约束的序列化空间定位问题**。
*   **核心思想**：不再将定位视为独立的点击预测，而是将其视为在动态演变的界面状态下的一系列相互依赖的空间决策。
*   **关键技术细节**：
    *   **数据构造**：从 10 个临床软件中录制真实交互视频，提取关键帧并标注。每个任务包含 2-3 个具有因果依赖的步骤。
    *   **标注结构**：每个步骤包含 GUI 截图 ($I_t$)、自然语言指令 ($s_t$)、语义目标描述 ($y_t$) 和归一化边界框 ($B_t$)。
    *   **评估协议（核心创新）**：引入**早期停止（Early-termination）机制**。如果模型在任务的某一步预测错误，则该任务后续所有步骤均判定为失败。这模拟了真实操作中一步错步步错的连锁反应。
    *   **失败分类法**：定义了六种失败类型：边缘偏差（Edge Bias）、远距失误（Far Miss）、工具栏混淆（Toolbar Confusion）、近距失误（Near Miss）、无预测（No Prediction）和小目标错误（Small Target）。

#### 3. 实验设计
*   **数据集/场景**：涵盖 10 个开源临床影像平台（如 3D Slicer, RadiAnt, ITK-SNAP 等），涉及 CT、MRI、PET、X光等多种模态。
*   **任务类型**：包括查看/显示、导入/加载、导出/保存、导航/缩放、标注/测量、工具调整和设置配置。
*   **对比方法**：评估了 **16 个 SOTA 模型**，分为三类：
    1.  **闭源模型**：GPT-5, GPT-4o-mini。
    2.  **通用开源 MLLM**：Llama 3.2, Qwen2-VL, DeepSeek-VL2, Gemma 3, Mistral-3 等。
    3.  **GUI 专用模型**：GUI-Actor, UI-TARS, OS-Atlas, CogAgent, AGUVIS 等。
*   **衡量指标**：任务完成准确率（TCA）、步骤命中率（SHR）、第一步准确率（S1A）和加权前缀得分（WPS）。

#### 4. 资源与算力
*   **硬件使用**：
    *   **开源模型**（除 Mistral 和 DeepSeek 外）：在单张 **NVIDIA A10 (24GB)** 上评估。
    *   **大型开源模型**（Mistral-3, DeepSeek-VL2）：在单张 **NVIDIA H100 (100GB)** 上评估。
*   **训练时长**：文中未提及训练时长，因为该论文主要关注基准测试的评估而非模型训练。
*   **推理配置**：输入图像保持原生分辨率，不进行缩放，以保证空间精度。

#### 5. 实验数量与充分性
*   **实验规模**：基准包含 216 个任务视频和 597 个标注关键帧。虽然规模不算巨大，但每个关键帧都经过了严格的人工校验。
*   **实验组数**：对 16 个模型进行了全量评估，并针对软件难度、目标大小、评估协议（序列 vs 单步）进行了多组对比实验。
*   **充分性与客观性**：
    *   使用了**自助法（Bootstrapping）**计算 95% 置信区间，验证了统计稳定性。
    *   进行了**消融实验**，证明了序列评估比单步评估更能揭示模型的脆弱性。
    *   通过**跨软件对比**，展示了模型在不同界面复杂度下的泛化能力。实验设计客观，对比基准统一。

#### 6. 主要结论与发现
*   **通用模型集体“翻车”**：Llama 3.2, Qwen2-VL 等通用模型在任务完成率（TCA）上几乎为 0%，无法处理医疗 GUI 的专业性和高精度要求。
*   **序列脆弱性**：即使是表现最好的模型（GUI-Actor），其第一步准确率（S1A）虽有 65%，但最终任务完成率（TCA）仅为 43.5%，说明错误在工作流中快速传播。
*   **GUI 专用模型占优**：专门为界面交互设计的模型（如 GUI-Actor, UI-TARS）显著优于通用模型和闭源模型（如 GPT-5）。
*   **小目标是瓶颈**：所有模型在面对密集的微小图标（如 DICOM 查看器的工具栏）时表现均大幅下降。

#### 7. 优点：亮点与创新
*   **填补空白**：首次针对临床软件 GUI 提出了工作流感知的序列化定位基准。
*   **评估严苛且真实**：提出的“首错即止”协议比传统的独立步骤评估更符合安全关键（Safety-critical）领域的实际需求。
*   **诊断深入**：详细的失败分类法不仅给出了分数，还分析了模型为什么失败（如边缘偏差或工具栏混淆），为后续改进提供了方向。
*   **跨平台多样性**：覆盖 10 种不同风格的医疗软件，保证了基准的鲁棒性。

#### 8. 不足与局限
*   **操作类型受限**：目前仅支持“点击（Click）”操作，未包含拖拽、滚动、文本输入等复杂交互。
*   **语言单一**：目前仅支持英文指令。
*   **数据规模**：216 个任务对于深度学习训练来说规模较小，目前主要作为测试集使用。
*   **静态局限**：评估基于静态关键帧，无法完全捕捉动态加载、弹窗遮挡等实时交互中的随机状态变化。
*   **风险权重缺失**：目前的评估对所有错误一视同仁，但在医疗领域，点击“删除病人记录”的错误远比点击错“缩放图标”严重。

（完）
