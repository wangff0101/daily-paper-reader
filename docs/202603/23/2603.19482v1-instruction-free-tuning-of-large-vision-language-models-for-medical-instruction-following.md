---
title: Instruction-Free Tuning of Large Vision Language Models for Medical Instruction Following
title_zh: 面向医疗指令遵循的大视觉语言模型无指令微调
authors: "Myeongkyun Kang, Soopil Kim, Xiaoxiao Li, Sang Hyun Park"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19482v1"
tags: ["query:medllm"]
score: 9.0
evidence: 医学领域视觉语言模型的免指令微调
tldr: 针对医疗领域构建高质量指令数据集困难的问题，本文提出一种无需指令的微调方法。该方法仅利用图像-描述对，通过动量代理指令替代手工指令，并结合响应打乱策略，在保留预训练模型指令遵循能力的同时，实现了高效的领域自适应。实验证明，该方法在多个医疗视觉问答任务上达到了最先进水平。
selection_source: fresh_fetch
motivation: 医疗领域缺乏大规模高质量的指令微调数据集，且专家标注成本极高。
method: 提出动量代理指令替代手工指令，并引入响应打乱策略以减少模型对前文的过度依赖。
result: 在SKINCON、WBCAtt、CBIS和MIMIC-CXR等多个医疗数据集的视觉问答任务中取得了最先进的准确率。
conclusion: 该方法显著提升了医疗领域大视觉语言模型的微调效率，证明了无需显式指令也能实现有效的领域指令遵循。
---

## 摘要
大视觉语言模型（LVLMs）在广泛的任务中展现出了令人印象深刻的性能。这些能力主要源于视觉指令微调，即在由精心策划的“图像-指令-输出”三元组组成的数据集上对模型进行微调。然而，在医疗领域，由于需要专门的专家知识，构建大规模、高质量的指令数据集尤其具有挑战性。为了解决这一问题，我们提出了一种无指令微调方法，该方法减少了对手工指令的依赖，仅利用“图像-描述”对进行微调。具体而言，我们引入了动量代理指令（momentum proxy instruction）来替代精心策划的文本指令，这在保留预训练 LVLM 指令遵循能力的同时，促进了在推理过程中保持有效的参数更新。因此，即使在微调过程中缺乏显式指令，微调后的 LVLM 也能灵活地响应特定领域的指令。此外，我们结合了响应打乱策略，以减轻模型对先前词汇的过度依赖，从而促进更有效的微调。我们的方法在 SKINCON、WBCAtt、CBIS 和 MIMIC-CXR 数据集的选择题视觉问答任务上达到了最先进的准确率，显著提升了 LVLMs 在医疗领域的微调效率。

## Abstract
Large vision language models (LVLMs) have demonstrated impressive performance across a wide range of tasks. These capabilities largely stem from visual instruction tuning, which fine-tunes models on datasets consisting of curated image-instruction-output triplets. However, in the medical domain, constructing large-scale, high-quality instruction datasets is particularly challenging due to the need for specialized expert knowledge. To address this issue, we propose an instruction-free tuning approach that reduces reliance on handcrafted instructions, leveraging only image-description pairs for fine-tuning. Specifically, we introduce a momentum proxy instruction as a replacement for curated text instructions, which preserves the instruction-following capability of the pre-trained LVLM while promoting updates to parameters that remain valid during inference. Consequently, the fine-tuned LVLM can flexibly respond to domain-specific instructions, even though explicit instructions are absent during fine-tuning. Additionally, we incorporate a response shuffling strategy to mitigate the model's over-reliance on previous words, facilitating more effective fine-tuning. Our approach achieves state-of-the-art accuracy on multiple-choice visual question answering tasks across SKINCON, WBCAtt, CBIS, and MIMIC-CXR datasets, significantly enhancing the fine-tuning efficiency of LVLMs in medical domains.

---

## 论文详细总结（自动生成）

这篇论文提出了一种针对医疗领域大视觉语言模型（LVLM）的“无指令微调”（Instruction-Free Tuning）框架，旨在解决医疗指令数据获取成本高、质量难以保证的问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的视觉指令微调高度依赖“图像-指令-输出”三元组。在医疗领域，构建高质量指令集需要极高的专家成本，且利用通用大模型（如 GPT-4）自动生成的指令往往存在专业术语误用、幻觉以及对上下文理解不足等问题。
*   **研究动机**：医疗影像通常自带详细的文本描述（如放射科报告）。论文试图探索是否可以**仅利用现有的“图像-描述”对**进行微调，同时保留模型对多样化人类指令的遵循能力，避免模型在微调后出现“指令依赖”（仅能响应特定指令）或“图像依赖”（忽略指令，输出固定格式）的退化现象。

### 2. 方法论
论文提出了 **InstFree** 框架，包含两个核心技术：
*   **动量代理指令（Momentum Proxy Instruction）**：
    *   **核心思想**：在微调阶段，不使用具体的文本指令（如“请描述这张图”），而是引入一组可学习的连续向量作为“代理指令”。
    *   **技术细节**：这些向量与模型参数共同优化。为了防止模型对特定代理指令过拟合，引入了**指数移动平均（EMA）**机制来更新代理指令（即动量更新）。
    *   **作用**：在推理阶段，这些代理向量会被丢弃，换回自然的文本指令。这种方式能保护预训练模型的指令遵循逻辑，同时让视觉编码器学习到领域相关的特征。
*   **响应打乱策略（Response Shuffling）**：
    *   **核心思想**：医疗报告通常有固定的逻辑结构（如先描述病灶再给出诊断），模型容易产生强烈的词汇依赖，从而忽略视觉特征。
    *   **技术细节**：在训练时，随机打乱原始描述中句子的顺序（以逗号或句号为分隔符）。
    *   **作用**：迫使模型更多地关注图像内容而非预测文本序列的统计规律，从而提高视觉问答（VQA）的准确性。

### 3. 实验设计
*   **数据集**：涵盖了四个不同领域的医疗数据集：
    1.  **SKINCON**：皮肤病学影像。
    2.  **WBCAtt**：白细胞显微影像。
    3.  **CBIS**：乳腺钼靶影像。
    4.  **MIMIC-CXR**：胸部 X 光报告。
*   **Benchmark**：将测试集转化为**多选题视觉问答（Multiple-choice VQA）**任务，以客观衡量模型的理解能力。
*   **对比方法**：
    *   **零样本/预训练模型**：BLIP-2, MedGemma-4B, PubMedVision-7B, Qwen2.5-VL-3B, LLaMA-3.2-11B-Vision。
    *   **全量微调模型**：使用固定文本指令进行微调的上述模型。

### 4. 资源与算力
*   **模型基础**：基于 LLaMA-3.2-11B-Vision-Instruct。
*   **训练框架**：使用 PyTorch 的 `torchtune` 库。
*   **超参数**：输入分辨率 560x560，Batch Size 为 2，使用 AdamW 优化器。代理指令向量数量 $N=8$，动量系数 $\alpha=0.999$。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量，但提到微调过程分为 1 个 Epoch 的预热阶段和 5 个 Epoch 的正式微调阶段。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个大型医疗数据集上进行了测试，每项实验均运行 3 次并取平均值。
*   **消融实验**：非常充分。分别针对动量系数 $\alpha$、代理指令向量数量 $N$、响应打乱策略的有效性、以及代理指令的不同更新方式（固定 vs 实时更新）进行了详细对比。
*   **客观性**：通过多选题形式进行评估，避免了开放式生成评价的主观性；对比了当前最先进的医疗专用模型（如 MedGemma），证明了方法的竞争力。

### 6. 主要结论与发现
*   **性能领先**：InstFree 在所有数据集上均显著优于未微调模型和传统指令微调模型，平均准确率提升显著（如在 SKINCON 上从 39.9% 提升至 75.2%）。
*   **解决指令依赖**：传统微调模型在面对未见过的指令格式时表现极差，而 InstFree 能够灵活响应各种问答指令。
*   **视觉增强**：响应打乱策略有效减少了模型对文本惯性的依赖，显著提升了对关键属性（如细胞类型、病灶性质）的识别精度。

### 7. 优点
*   **高效性**：无需人工编写或 LLM 生成复杂的指令数据集，直接利用原始报告即可训练。
*   **鲁棒性**：动量代理指令成功解决了微调过程中预训练能力的流失问题。
*   **通用性**：在皮肤、血液、乳腺、胸部 X 光等多个差异巨大的医疗领域均表现稳健。

### 8. 不足与局限
*   **分隔符敏感性**：响应打乱策略依赖于文本的正确分割（如逗号、句号），对于格式极度混乱的原始报告，效果可能会打折扣。
*   **推理开销**：虽然训练时使用了代理指令，但模型规模（11B）对于某些实时医疗应用场景可能仍然偏大。
*   **评估局限**：主要集中在多选题 VQA，对于复杂的长文本医疗报告生成的质量评估相对较少。

（完）
