---
title: Guideline-grounded retrieval-augmented generation for ophthalmic clinical decision support
title_zh: 基于指南的检索增强生成用于眼科临床决策支持
authors: "Shuying Chen, Sen Cui, Zhong Cao"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21925v1"
tags: ["query:medllm"]
score: 10.0
evidence: 用于眼科临床决策支持与诊断的检索增强生成系统
tldr: 本研究提出Oph-Guid-RAG，一种针对眼科临床决策支持的多模态视觉检索增强生成系统。该系统将指南页面作为独立证据单元进行图像检索，有效保留了表格和流程图等布局信息，并设计了包含路由与过滤的可控检索框架以减少噪声。实验表明，该方法在处理复杂临床问题时显著优于GPT系列模型，提供了具有可追溯性的证据支持，显著增强了临床AI的鲁棒性与准确性。
selection_source: fresh_fetch
motivation: 针对眼科临床指南中大量表格和流程图难以被传统文本RAG有效利用的问题，探索多模态证据检索方案。
method: 开发了集成查询分解、页面级图像检索、重排序及多模态推理的可控RAG框架，并提供指南页面引用。
result: "在HealthBench硬子集上，该方法将综合评分提升了30%，准确率相比基座模型有显著增长。"
conclusion: 视觉检索与可控推理的结合能显著改善临床AI的证据扎根能力，是提升复杂医疗决策支持质量的关键。
---

## 摘要
在这项工作中，我们提出了 Oph-Guid-RAG，这是一个用于眼科临床问答和决策支持的多模态视觉 RAG 系统。我们将每个指南页面视为一个独立的证据单元，并直接检索页面图像，从而保留了表格、流程图和布局信息。我们进一步设计了一个带有路由和过滤功能的可控检索框架，该框架能够选择性地引入外部证据并减少噪声。该系统集成了查询分解、查询重写、检索、重排序和多模态推理，并提供带有指南页面引用的可追溯输出。我们在 HealthBench 上使用基于医生的评分协议评估了我们的方法。在困难子集上，与 GPT-5.2 相比，我们的方法将总分从 0.2969 提高到 0.3861（+0.0892，+30.0%），并实现了更高的准确率，从 0.5956 提高到 0.6576（+0.0620，+10.4%）。与 GPT-5.4 相比，我们的方法实现了更大的准确率提升，达到 +0.1289（+24.4%）。这些结果表明，我们的方法在需要精确、基于证据的推理的挑战性案例上更为有效。消融研究进一步表明，重排序、路由和检索设计对于稳定性能至关重要，尤其是在困难设置下。总之，我们展示了将基于视觉的检索与可控推理相结合如何提高临床 AI 应用中的证据基础和鲁棒性，同时也指出需要进一步的工作来使其更加完善。

## Abstract
In this work, we propose Oph-Guid-RAG, a multimodal visual RAG system for ophthalmology clinical question answering and decision support. We treat each guideline page as an independent evidence unit and directly retrieve page images, preserving tables, flowcharts, and layout information. We further design a controllable retrieval framework with routing and filtering, which selectively introduces external evidence and reduces noise. The system integrates query decomposition, query rewriting, retrieval, reranking, and multimodal reasoning, and provides traceable outputs with guideline page references. We evaluate our method on HealthBench using a doctor-based scoring protocol. On the hard subset, our approach improves the overall score from 0.2969 to 0.3861 (+0.0892, +30.0%) compared to GPT-5.2, and achieves higher accuracy, improving from 0.5956 to 0.6576 (+0.0620, +10.4%). Compared to GPT-5.4, our method achieves a larger accuracy gain of +0.1289 (+24.4%). These results show that our method is more effective on challenging cases that require precise, evidence-based reasoning. Ablation studies further show that reranking, routing, and retrieval design are critical for stable performance, especially under difficult settings. Overall, we show how combining visionbased retrieval with controllable reasoning can improve evidence grounding and robustness in clinical AI applications,while pointing out that further work is needed to be more complete.

---

## 论文详细总结（自动生成）

以下是对论文《Guideline-grounded retrieval-augmented generation for ophthalmic clinical decision support》（基于指南的检索增强生成用于眼科临床决策支持）的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：眼科临床决策高度依赖实践指南，这些指南包含复杂的治疗路径、药物剂量和转诊标准。
*   **现有挑战**：
    *   **传统 RAG 的局限性**：临床指南中充斥着大量表格、流程图和复杂布局，传统的 OCR（光学字符识别）和文本切片技术容易导致信息丢失或结构破坏。
    *   **模型幻觉**：通用大模型（LLM）仅依赖参数化知识，在处理罕见病或复杂病例时容易产生错误的阈值、分级或禁忌症。
*   **核心目标**：开发一种名为 **Oph-Guid-RAG** 的系统，通过直接检索指南的“页面图像”而非纯文本，保留视觉结构信息，并引入可控的推理框架来提升临床问答的准确性、鲁棒性和可追溯性。

### 2. 方法论
系统采用四阶段的多模态视觉 RAG 架构：
*   **阶段 I：语料库准备（视觉索引）**：
    *   将 305 份眼科指南（共 7001 页）转换为高分辨率图像（720 DPI）。
    *   **核心思想**：不进行 OCR，将每一页视为独立的证据单元。
    *   使用 **ColQwen2.5** 作为视觉文档检索器，将页面图像和文本查询映射到统一的向量空间，并利用 **FAISS** 构建索引。
*   **阶段 II：查询处理（可控路由）**：
    *   **Planner（规划器）**：将复杂的多意图查询分解为最多 3 个子问题。
    *   **Router（路由器）**：判断子问题是否需要检索（RAG 路径）或直接回答（DIRECT 路径）。
    *   **Query Rewrite（重写）**：将子问题转化为更适合检索的临床实体描述。
*   **阶段 III：检索与过滤**：
    *   执行视觉检索获取 Top-K 候选页面。
    *   **LLM 过滤器**：使用 GPT-5.2 对检索到的页面进行二次审核，剔除无关噪声。若无相关证据，则回退至 DIRECT 路径。
*   **阶段 IV：生成与合成**：
    *   **多模态推理**：将文本查询与检索到的页面图像同时输入视觉语言模型（VLM）生成答案。
    *   **最终合成**：整合所有子问题的答案，并附带指南页面的 URL/图像引用，确保结果可审计。

### 3. 实验设计
*   **数据集**：使用 **HealthBench**（一个基于医生评估准则的基准测试）。
*   **子集构建**：通过关键词过滤提取了 78 个眼科相关提示词，分为 **Consensus（共识）** 和 **Hard（困难）** 两个子集。
*   **对比方法**：将 Oph-Guid-RAG 与基座模型 **GPT-5.2、GPT-5.3、GPT-5.4** 进行对比。
*   **评估指标**：采用医生编写的量表，涵盖准确性（Accuracy）、完整性（Completeness）、上下文感知（Context awareness）、沟通质量（Communication quality）和指令遵循（Instruction following）。

### 4. 资源与算力
*   **算力支持**：文中提到使用了“中国医学科学院高性能计算平台”提供计算支持。
*   **具体细节**：论文**未明确说明**具体的 GPU 型号（如 A100 或 H100）、GPU 数量、训练时长或推理延迟等硬件参数。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在全量集（78 个样本）和硬子集（16 个样本）上进行了性能对比。
    *   针对 **重排序（Reranking）、查询重写（Query Rewrite）和路由器（Router）** 进行了详细的消融实验。
*   **充分性评价**：
    *   实验设计较为客观，使用了官方的 HealthBench 评分脚本和统一的系统提示词，确保了对比的公平性。
    *   消融实验清晰地展示了各模块在不同难度任务下的贡献度。
    *   **不足之处**：样本量（尤其是 Hard 子集仅 16 个）相对较小，可能存在一定的统计随机性。

### 6. 主要结论与发现
*   **性能提升**：在 Hard 子集上，Oph-Guid-RAG 的总分比 GPT-5.2 提升了 **30%**，准确率提升了 **10.4%**；相比 GPT-5.4，准确率提升了 **24.4%**。
*   **模块作用**：消融实验显示，**重排序（Reranking）** 是提升性能最关键的组件，尤其在处理复杂临床问题时，移除该模块会导致准确率大幅下降。
*   **权衡关系**：引入 RAG 显著提升了准确性和上下文感知能力，但在**完整性（Completeness）**上有所下降，这表明系统在利用证据时倾向于保守，以牺牲篇幅来换取事实准确性。

### 7. 优点
*   **视觉优先**：通过页面级图像检索，完美解决了 PDF 中表格和流程图难以解析的痛点。
*   **可控性强**：设计的路由和过滤机制有效减少了无关信息的干扰，降低了 RAG 系统常见的“噪声引入”风险。
*   **高可追溯性**：系统不仅给出答案，还直接展示原始指南页面，符合临床医疗对“证据扎根”的严格要求。

### 8. 不足与局限
*   **完整性缺陷**：实验显示其回答的详尽程度（Completeness）低于基座模型，可能导致临床建议不够全面。
*   **时效性风险**：系统依赖于预构建的指南语料库，若指南发生更新或存在地域差异，系统可能无法实时同步。
*   **应用限制**：目前仅作为决策支持工具，不能独立进行诊断或处方，且对复杂多轮对话的处理仍有提升空间。
*   **评估局限**：依赖于自动化 LLM 评分器（虽然基于医生准则），可能与真实人类医生的主观判断存在偏差。

（完）
